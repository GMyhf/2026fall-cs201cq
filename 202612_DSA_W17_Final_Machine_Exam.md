# 第17周 期末上机考试（命题方案与建议考查内容）

*Updated 2026-08-30 15:00 GMT+8*
 *Compiled by Hongfei Yan (2026 Fall)*
https://github.com/GMyhf/2026fall-cs201cq

> **大纲对应**：第 17 周 / 3 学时
> **教学内容**：期末上机考试
> **教学要求**：综合运用所学解决算法问题
> **考核占比**：总评 **60%**

**本文件面向教师**：给出上机考试的命题原则、知识点覆盖矩阵、一套完整样卷（6 题，含题面、样例、参考解答、数据构造建议、评分标准）以及备选题库。
**学生可用部分**：第 1 节考试须知、第 2 节能力要求、第 6 节备考建议。

---

# 1 考试须知

| 项目 | 规定 |
| ---- | ---- |
| 时长 | **120 分钟** |
| 题量 | **6 道**算法编程题 |
| 总分 | 100 分（见第 4 节分值分配） |
| 平台 | OJ 在线评测（OpenJudge / 校内 OJ） |
| 语言 | Python 3 为主，允许 C++ |
| 环境 | 考试专用机房，**断开外网** |
| 允许携带 | **一页 A4 双面手写 cheat sheet**（不得打印、不得夹带整页代码） |
| **禁止** | ⚠️ **任何 AI 工具**（含本地模型、IDE 智能补全插件）、任何联网查询、任何形式的交流 |
| 学术诚信 | **无法解释自己提交代码者，按学术不端处理**，成绩记 0 |

**判分方式**：以 OJ 通过（AC）情况为准，按题给分；部分题设置分档数据（部分通过给部分分）。

---

# 2 能力要求

本次考试考查以下五项能力，命题时每道题至少覆盖其中两项：

1. **问题抽象**：把文字描述翻译成数据结构模型（这是最常见的失分点）。
2. **算法选择**：根据数据范围选出复杂度可行的算法。
3. **代码实现**：在无 AI 辅助下正确、快速地写出可运行代码。
4. **边界处理**：n=0/1、多组数据、重边自环、不连通、溢出等。
5. **调试能力**：读懂 WA/TLE 反馈并定位问题。

---

# 3 知识点覆盖矩阵

命题时保证 6 道题覆盖以下**至少 5 个大类**，且**线性结构、树、图、DP 各至少 1 题**：

| 大类 | 周次 | 本卷覆盖 | 具体考点 |
| ---- | ---- | ---- | ---- |
| 基础与复杂度 | W2–W3 | T1 | 哈希表、前缀和、复杂度判断 |
| 线性结构 | W4–W5 | T2 | 栈 / 队列 / 单调栈 / 链表 |
| 排序与分治 | W6 | T3 | 排序、二分、贪心 |
| 贪心与 DP | W7 | T5 | 线性 DP / 背包 |
| 搜索 | W8 | T4 或 T6 | BFS / DFS / 回溯 |
| 树与堆 | W9–W11 | T4 | 遍历、建树、堆、并查集 |
| 图论 | W12–W14 | T6 | 最短路 / MST / 拓扑排序 |
| 字符串与哈希 | W15 | T1 备选 | KMP / Trie / 倒排 |

**难度梯度建议**：

```
T1  ★☆☆☆☆   签到题，90% 学生应 AC        —— 保证及格线
T2  ★★☆☆☆   基础数据结构应用，70% AC
T3  ★★★☆☆   需要一次转化，55% AC
T4  ★★★☆☆   树 / 堆综合，45% AC
T5  ★★★★☆   DP，状态设计有难度，30% AC
T6  ★★★★★   图论综合 / 建模，15% AC       —— 区分优秀
```

> **命题原则**：前 3 题保证认真上课听讲、完成作业的学生能拿到 45–50 分；后 3 题拉开区分度。**AC 5–6 题对应优秀，AC 4 题为良好，AC 2–3 题为及格。**

---

# 4 建议样卷（一套完整试题）

**分值分配**：T1 15 分 + T2 15 分 + T3 15 分 + T4 15 分 + T5 20 分 + T6 20 分 = **100 分**

---

## T1. 课程签到统计（15 分）

**考点**：哈希表、排序、字符串处理（W2、W3、W15）　　**难度**：★☆☆☆☆

### 题面

某课程有 n 次签到记录，每条记录是一个学生姓名（仅含小写英文字母，长度 ≤ 20）。请统计每个学生的签到次数，并按**签到次数从多到少**输出；次数相同时按**姓名字典序从小到大**输出。

**输入**
第一行一个整数 n（1 ≤ n ≤ 2×10⁵）。
接下来 n 行，每行一个学生姓名。

**输出**
若干行，每行为 `姓名 次数`。

**样例输入**
```
6
alice
bob
alice
carol
bob
alice
```

**样例输出**
```
alice 3
bob 2
carol 1
```

### 参考解答

```python
import sys
from collections import Counter


def main():
    data = sys.stdin.read().split()
    n = int(data[0])
    cnt = Counter(data[1:1 + n])
    items = sorted(cnt.items(), key=lambda x: (-x[1], x[0]))
    sys.stdout.write('\n'.join(f"{name} {c}" for name, c in items) + '\n')


main()
```

**复杂度**：O(n log n)。

### 数据构造建议

- 数据 1–2：n ≤ 10，手工构造，含次数相同需按字典序排的情况。
- 数据 3–4：n = 2×10⁵，随机 1000 个不同姓名 —— **卡掉用 `list.count()` 的 O(n²) 写法**。
- 数据 5：n = 2×10⁵，全部相同姓名（边界）。

### 评分标准

AC 得满分 15 分。常见错误：多关键字排序写反、用 `list` 统计导致 TLE。

---

## T2. 表达式求值（15 分）

**考点**：栈、调度场算法、表达式转换（W4）　　**难度**：★★☆☆☆

### 题面

给定一个只含非负整数、`+ - * /` 和圆括号的中缀表达式（可能含空格），求其值。除法为**整数除法向零取整**，保证不出现除零，中间结果与最终结果的绝对值不超过 10⁹。

**输入**
第一行一个整数 T（1 ≤ T ≤ 100），表示表达式个数。
接下来 T 行，每行一个表达式，长度 ≤ 1000。

**输出**
T 行，每行一个整数。

**样例输入**
```
3
1 + 2 * 3
(1 + 2) * 3
100 / 7 - 2
```

**样例输出**
```
7
9
12
```

### 参考解答

```python
import sys

PREC = {'+': 1, '-': 1, '*': 2, '/': 2}


def tokenize(expr):
    tokens, i, n = [], 0, len(expr)
    while i < n:
        if expr[i].isspace():
            i += 1
        elif expr[i].isdigit():
            j = i
            while j < n and expr[j].isdigit():
                j += 1
            tokens.append(expr[i:j])
            i = j
        else:
            tokens.append(expr[i])
            i += 1
    return tokens


def to_postfix(tokens):
    out, ops = [], []
    for tk in tokens:
        if tk.isdigit():
            out.append(tk)
        elif tk == '(':
            ops.append(tk)
        elif tk == ')':
            while ops[-1] != '(':
                out.append(ops.pop())
            ops.pop()
        else:
            while ops and ops[-1] != '(' and PREC[ops[-1]] >= PREC[tk]:
                out.append(ops.pop())
            ops.append(tk)
    while ops:
        out.append(ops.pop())
    return out


def eval_postfix(tokens):
    st = []
    for tk in tokens:
        if tk.isdigit():
            st.append(int(tk))
        else:
            b, a = st.pop(), st.pop()
            if tk == '+':
                st.append(a + b)
            elif tk == '-':
                st.append(a - b)
            elif tk == '*':
                st.append(a * b)
            else:
                st.append(int(a / b))      # 向零取整
    return st[0]


def main():
    data = sys.stdin.read().split('\n')
    t = int(data[0])
    out = []
    for i in range(1, t + 1):
        out.append(str(eval_postfix(to_postfix(tokenize(data[i])))))
    print('\n'.join(out))


main()
```

**复杂度**：O(总长度)。

### 数据构造建议

- 含多层嵌套括号；含连续减法 `10-3-2`（测左结合）；含 `a/b` 负数向零取整；含大量空格。

### 评分标准

AC 15 分。设置分档：仅含 `+ *` 的子任务给 8 分（便于部分学生得分）。
常见错误：左结合写成 `>` 导致 `10-3-2` 算错；整除用 `//` 导致负数结果错误。

---

## T3. 最小化最大装载量（15 分）

**考点**：二分答案 + 贪心验证（W3、W6、W7）　　**难度**：★★★☆☆

### 题面

有 n 个货物排成一列，第 i 个重 wᵢ。要把它们**按原顺序**分成恰好 k 组（每组是连续的一段，不能为空），使得**最重的那一组的重量最小**。求这个最小值。

**输入**
第一行两个整数 n, k（1 ≤ k ≤ n ≤ 10⁵）。
第二行 n 个整数 wᵢ（1 ≤ wᵢ ≤ 10⁴）。

**输出**
一个整数。

**样例输入**
```
5 2
1 2 3 4 5
```

**样例输出**
```
9
```
（分成 `[1,2,3,4]` 和 `[5]` 得 10；分成 `[1,2,3]` 和 `[4,5]` 得 9，最优。）

### 参考解答

```python
import sys


def main():
    data = sys.stdin.read().split()
    n, k = int(data[0]), int(data[1])
    w = [int(x) for x in data[2:2 + n]]

    def groups_needed(limit):
        """在每组和不超过 limit 的约束下，贪心最少需要多少组。"""
        cnt, cur = 1, 0
        for x in w:
            if cur + x > limit:
                cnt += 1
                cur = x
            else:
                cur += x
        return cnt

    lo, hi = max(w), sum(w)          # 答案的取值区间
    while lo < hi:
        mid = (lo + hi) // 2
        if groups_needed(mid) <= k:  # 能用 <= k 组做到，说明 limit 还能更小
            hi = mid
        else:
            lo = mid + 1
    print(lo)


main()
```

**复杂度**：O(n log(Σw))。

### 数据构造建议

- k = 1（答案 = 总和）、k = n（答案 = 最大值）两个边界。
- n = 10⁵ 随机数据 —— 卡掉 O(n²k) 的朴素 DP。
- 全部相同的 w。

### 评分标准

AC 15 分。分档：n ≤ 100 的子任务（允许 O(n²k) DP）给 7 分。
常见错误：二分区间下界写成 0（导致 `groups_needed` 死循环）；判定条件方向写反。

---

## T4. 树的重构与查询（15 分）

**考点**：二叉树建树、遍历、树高（W9）　　**难度**：★★★☆☆

### 题面

给定一棵二叉树的**前序遍历**与**中序遍历**序列（结点值为互不相同的正整数），请输出：

1. 该树的**后序遍历**序列；
2. 该树的**高度**（只有根时高度为 1）；
3. 该树的**叶结点个数**。

**输入**
第一行一个整数 n（1 ≤ n ≤ 10⁵）。
第二行 n 个整数，前序遍历。
第三行 n 个整数，中序遍历。

**输出**
第一行：后序遍历，用空格分隔。
第二行：高度。
第三行：叶结点个数。

**样例输入**
```
5
1 2 4 5 3
4 2 5 1 3
```

**样例输出**
```
4 5 2 3 1
3
3
```

### 参考解答

```python
import sys


def main():
    sys.setrecursionlimit(1 << 20)
    data = sys.stdin.read().split()
    n = int(data[0])
    pre = [int(x) for x in data[1:1 + n]]
    ino = [int(x) for x in data[1 + n:1 + 2 * n]]
    pos = {v: i for i, v in enumerate(ino)}     # O(1) 定位根，避免 index() 的 O(n)

    post = []
    it = iter(pre)
    leaves = 0

    def build(lo, hi):
        """返回子树高度，同时把后序结果写入 post。"""
        nonlocal leaves
        if lo > hi:
            return 0
        val = next(it)
        mid = pos[val]
        hl = build(lo, mid - 1)
        hr = build(mid + 1, hi)
        post.append(val)                         # 左右都处理完再记录 -> 后序
        if hl == 0 and hr == 0:
            leaves += 1
        return 1 + max(hl, hr)

    height = build(0, n - 1)
    out = [' '.join(map(str, post)), str(height), str(leaves)]
    sys.stdout.write('\n'.join(out) + '\n')


main()
```

**复杂度**：O(n)。

> ⚠️ **注意**：`build` 里必须先递归左子树再递归右子树，因为 `next(it)` 按前序顺序取值，顺序不能错。

> ⚠️ **深递归的兜底写法**：链状树的递归深度可达 10⁵。部分评测环境即使调高 `setrecursionlimit`
> 也会因 C 栈溢出而 RE，标准做法是放到大栈线程里跑：
>
> ```python
> import threading, sys
> sys.setrecursionlimit(1 << 20)
> threading.stack_size(1 << 26)          # 64 MB 栈
> t = threading.Thread(target=main)
> t.start(); t.join()
> ```
>
> 命题时应在**目标评测环境上实测**链状数据，据此决定是否放宽数据（如把 n 降到 10⁴）或在题面中提示。

### 数据构造建议

- n = 1（单结点）。
- **链状树**（前序 = 中序 = 1..n 的退化情形）—— 测递归深度，卡掉未调 `setrecursionlimit` 的提交。
- n = 10⁵ 随机树 —— 卡掉每层用 `ino.index()` 的 O(n²) 写法。

### 评分标准

AC 15 分。分档：n ≤ 1000 子任务给 8 分。
常见错误：递归爆栈；用 `list.index` 导致 TLE；切片建树导致 O(n²) 空间。

---

## T5. 装箱方案数（20 分）

**考点**：01 背包 / 完全背包、方案计数（W7）　　**难度**：★★★★☆

### 题面

有 n 种物品，第 i 种体积为 vᵢ，**数量无限**。求恰好装满容量为 C 的背包的**方案数**（两个方案不同当且仅当至少有一种物品的使用数量不同，即**不计顺序**）。答案对 10⁹+7 取模。

**输入**
第一行两个整数 n, C（1 ≤ n ≤ 100，1 ≤ C ≤ 10⁴）。
第二行 n 个整数 vᵢ（1 ≤ vᵢ ≤ C）。

**输出**
一个整数。

**样例输入**
```
3 5
1 2 5
```

**样例输出**
```
4
```
（`1×5`、`1×3+2×1`、`1×1+2×2`、`5×1`）

### 参考解答

```python
import sys

MOD = 10 ** 9 + 7


def main():
    data = sys.stdin.read().split()
    n, C = int(data[0]), int(data[1])
    v = [int(x) for x in data[2:2 + n]]

    dp = [0] * (C + 1)
    dp[0] = 1                          # 容量 0 有 1 种方案：什么都不装
    for vi in v:                       # ⚠️ 外层枚举物品 -> 组合数（不计顺序）
        for c in range(vi, C + 1):     # ⚠️ 完全背包 -> 容量正序
            dp[c] = (dp[c] + dp[c - vi]) % MOD
    print(dp[C])


main()
```

**复杂度**：O(nC)。

### 变形（可作为另一档或替换题）

- 每种物品**只有一件** → 内层容量**倒序**（01 背包）。
- 计**排列数**（计顺序）→ 外层枚举容量、内层枚举物品。
- 求**最少物品件数** → `dp[c] = min(dp[c], dp[c-vi]+1)`，初始 `dp[0]=0` 其余 INF。

### 数据构造建议

- 含 vᵢ = C 的情形；含重复 vᵢ；C = 10⁴、n = 100 的满规模数据。
- **关键陷阱数据**：让"外层容量、内层物品"（排列数）写法输出错误答案的数据，例如 `n=2, C=3, v=[1,2]`：组合数为 2（`1+1+1`、`1+2`），排列数为 3。

### 评分标准

AC 20 分。分档：C ≤ 100 给 10 分。
常见错误：循环顺序写反（算成排列数）；倒序写成正序（01 与完全混淆）；忘记取模。

---

## T6. 城市网络维护（20 分）

**考点**：最小生成树 + 并查集 + 连通性判断（W11、W14）　　**难度**：★★★★★

### 题面

某地区有 n 个城市、m 条**候选**光缆线路，第 j 条连接城市 uⱼ 与 vⱼ，造价 wⱼ。其中前 k 条线路**已经建成**（造价已支付，无需再付费）。

请选择若干条尚未建成的线路，使得所有城市连通，且**新增造价总和最小**。若无论如何都无法让所有城市连通，输出 `impossible`。

**输入**
第一行三个整数 n, m, k（1 ≤ n ≤ 10⁵，0 ≤ k ≤ m ≤ 2×10⁵）。
接下来 m 行，每行三个整数 uⱼ vⱼ wⱼ（1 ≤ u, v ≤ n，0 ≤ w ≤ 10⁶）。**前 k 行为已建成线路。**

**输出**
一个整数（最小新增造价），或 `impossible`。

**样例输入**
```
4 5 1
1 2 10
2 3 5
3 4 7
1 4 100
1 3 3
```

**样例输出**
```
10
```

**样例说明**：第 1 条线路 `1-2`（造价 10）已建成，免费，此时 {1,2} 已连通。
在剩余候选线路中按造价升序选取：选 `1-3`(3) 连入城市 3；`2-3`(5) 因 2 与 3 已连通而跳过；选 `3-4`(7) 连入城市 4。
全图连通，新增造价 = 3 + 7 = **10**。

### 参考解答

```python
import sys


class DSU:
    def __init__(self, n):
        self.p = list(range(n))
        self.sz = [1] * n
        self.count = n

    def find(self, x):
        r = x
        while self.p[r] != r:
            r = self.p[r]
        while self.p[x] != r:
            self.p[x], x = r, self.p[x]
        return r

    def union(self, x, y):
        rx, ry = self.find(x), self.find(y)
        if rx == ry:
            return False
        if self.sz[rx] < self.sz[ry]:
            rx, ry = ry, rx
        self.p[ry] = rx
        self.sz[rx] += self.sz[ry]
        self.count -= 1
        return True


def main():
    data = sys.stdin.buffer.read().split()
    p = 0
    n, m, k = int(data[p]), int(data[p + 1]), int(data[p + 2])
    p += 3
    dsu = DSU(n + 1)
    dsu.count -= 1                     # 下标 0 不使用，不计入连通分量

    candidates = []
    for j in range(m):
        u, v, w = int(data[p]), int(data[p + 1]), int(data[p + 2])
        p += 3
        if j < k:
            dsu.union(u, v)            # 已建成：直接合并，不计费
        else:
            candidates.append((w, u, v))

    candidates.sort()                  # Kruskal：按造价升序
    total = 0
    for w, u, v in candidates:
        if dsu.count == 1:
            break
        if dsu.union(u, v):
            total += w

    print(total if dsu.count == 1 else "impossible")


main()
```

**复杂度**：O(m log m + m α(n))。

**核心思想**：已建成的线路相当于**造价为 0 的边**，先无条件合并；剩下的边跑标准 Kruskal。

### 数据构造建议

- k = 0（退化为普通 MST）、k = m（已全部建成，答案 0）。
- 图**不连通**的数据（输出 `impossible`）。
- 含**重边与自环**。
- n = 10⁵、m = 2×10⁵ 满规模 —— 卡掉 O(nm) 的朴素 Prim 与未做路径压缩的并查集。
- w = 0 的边（测试不要误判）。

### 评分标准

AC 20 分。分档：n ≤ 1000 且 m ≤ 5000 给 10 分。
常见错误：忘记处理不连通；已建成边未先合并；并查集未优化导致 TLE；用 `input()` 读 2×10⁵ 行导致 TLE。

---

# 5 备选题库（按知识点分类）

命题时可从下表替换样卷中的对应题目，保持难度与覆盖不变。

## 5.1 线性结构（W4–W5）

| 题目 | 来源 | 难度 | 考点 |
| ---- | ---- | ---- | ---- |
| 合法出栈序列 | OJ 22068 | ★★ | 栈模拟 |
| 中序转后序 | OJ 24591 | ★★ | 调度场 |
| 约瑟夫问题 | OJ 02746 | ★★ | deque / 递推 |
| 每日温度 | LC 739 | ★★★ | 单调栈 |
| 滑动窗口最大值 | LC 239 | ★★★ | 单调队列 |
| 柱状图中最大的矩形 | LC 84 | ★★★★ | 单调栈进阶 |

## 5.2 排序、二分与贪心（W6–W7）

| 题目 | 来源 | 难度 | 考点 |
| ---- | ---- | ---- | ---- |
| Ultra-QuickSort | OJ 02299 | ★★★ | 归并求逆序对 |
| 无重叠区间 | LC 435 | ★★★ | 区间贪心 |
| 分割数组的最大值 | LC 410 | ★★★★ | 二分答案 |
| 数组中的第 K 个最大元素 | LC 215 | ★★★ | 快速选择 / 堆 |

## 5.3 搜索（W8）

| 题目 | 来源 | 难度 | 考点 |
| ---- | ---- | ---- | ---- |
| 最大连通域面积 | OJ 18160 | ★★ | 网格 DFS |
| 寻宝 | OJ 19930 | ★★ | 网格 BFS |
| 腐烂的橘子 | LC 994 | ★★★ | 多源 BFS |
| 词梯 | OJ 28046 | ★★★★ | 状态空间 BFS |
| 马走日 | OJ 04123 | ★★★ | 回溯计数 |
| 八皇后 | OJ 02754 | ★★★ | 回溯剪枝 |

## 5.4 树与堆（W9–W11）

| 题目 | 来源 | 难度 | 考点 |
| ---- | ---- | ---- | ---- |
| 前中序建树 | OJ 22158 | ★★★ | 建树 |
| 括号嵌套树 | OJ 24729 | ★★★ | 多叉树解析 |
| 二叉树的直径 | LC 543 | ★★★ | 树形 DP |
| 数据流的中位数 | LC 295 | ★★★★ | 对顶堆 |
| 哈夫曼编码树 | OJ 22161 | ★★★★ | 堆 + 贪心 |
| 宗教信仰 | OJ 02524 | ★★ | 并查集 |
| 虫子的生活 | OJ 07734 | ★★★★ | 带权并查集 |

## 5.5 动态规划（W7）

| 题目 | 来源 | 难度 | 考点 |
| ---- | ---- | ---- | ---- |
| 打家劫舍 | LC 198 | ★★ | 线性 DP |
| 最长递增子序列 | LC 300 | ★★★ | LIS |
| 最长公共子序列 | LC 1143 | ★★★ | 二维 DP |
| 编辑距离 | LC 72 | ★★★★ | 二维 DP |
| 分割等和子集 | LC 416 | ★★★ | 01 背包 |
| 零钱兑换 | LC 322 | ★★★ | 完全背包 |

## 5.6 图论（W12–W14）

| 题目 | 来源 | 难度 | 考点 |
| ---- | ---- | ---- | ---- |
| 兔子与樱花 | OJ 05443 | ★★★★ | Dijkstra + 路径还原 |
| 网络延迟时间 | LC 743 | ★★★ | Dijkstra |
| Agri-Net | OJ 01258 | ★★★ | 稠密图 Prim |
| 兔子与星空 | OJ 05442 | ★★★ | Kruskal |
| 舰队、海域出击！ | OJ 09202 | ★★★ | 拓扑判环 |
| 课程表 II | LC 210 | ★★★ | 拓扑排序 |
| Subway | OJ 02502 | ★★★★★ | 建模 + Dijkstra |

## 5.7 字符串与哈希（W15）

| 题目 | 来源 | 难度 | 考点 |
| ---- | ---- | ---- | ---- |
| 找出字符串中第一个匹配项的下标 | LC 28 | ★★★ | KMP |
| 重复的子字符串 | LC 459 | ★★★ | next 数组 |
| 实现 Trie | LC 208 | ★★★ | 前缀树 |
| 字母异位词分组 | LC 49 | ★★ | 哈希键设计 |
| 最长连续序列 | LC 128 | ★★★ | set 技巧 |

---

# 6 给学生的备考建议

## 6.1 必须能默写的 12 个模板

见第 16 周讲义第 3 节：快速 IO、二分、单调栈、单调队列、并查集、Dijkstra、拓扑排序、Kruskal、网格 BFS、回溯、背包、KMP、二叉树迭代中序。

## 6.2 一页 A4 cheat sheet 的建议内容

**正面**：
- 复杂度速查表 + 数据范围反推表
- 并查集、Dijkstra、拓扑排序、Kruskal 四大模板
- 背包四种循环顺序口诀

**反面**：
- 二分边界写法（`bisect_left` / `bisect_right` / 二分答案）
- 树的三种遍历（递归 + 迭代）
- KMP 的 next 数组
- 常见失分点清单（多组数据、输出格式、递归深度、`pop(0)`）

## 6.3 考场流程

```
1. 开考先花 8–10 分钟通读 6 题，在草稿纸上写下每题的
   【数据范围 → 目标复杂度 → 候选算法】，并按预估难度排序。
2. 从最有把握的开始做，先 AC 3 题稳住及格线。
3. 每题先跑通样例再提交；WA 后先自查边界（n=1、空输入、多组数据）。
4. TLE 后先查是不是 input()/pop(0)/in list，再考虑换算法。
5. 剩 10 分钟停止写新题，检查已提交题的输出格式。
```

## 6.4 三条硬性提醒

1. **不要在第一题上耗超过 20 分钟**——它只值 15 分。
2. **看到 n ≤ 10⁵ 就不要写 O(n²)**，看到 n ≤ 400 才考虑 O(n³)。
3. **提交前把代码通读一遍**，确认你能向老师解释每一行——这是考核要求，也是学术诚信底线。

---

# 7 命题与阅卷检查清单（教师用）

**命题阶段**：

- [ ] 6 题覆盖至少 5 个知识大类，线性结构 / 树 / 图 / DP 各至少 1 题
- [ ] 难度梯度符合第 3 节的 AC 率预期
- [ ] 每题有**参考程序**并跑通全部数据
- [ ] 每题的**样例说明文字**与参考程序输出**逐字核对**
- [ ] 每题有针对"错误算法"的**卡时数据**（O(n²) 卡 TLE、边界卡 WA）
- [ ] 每题有 n=1 / 空 / 全相同 / 不连通 等边界数据
- [ ] Python 参考程序在**平台时限内留有 3 倍以上余量**（Python 比 C++ 慢，时限需相应放宽）
- [ ] 分档数据设置合理，保证中等水平学生能拿到部分分

**考试阶段**：

- [ ] 机房断网，禁用 AI 插件与本地模型
- [ ] cheat sheet 仅一页 A4 手写，考前统一检查
- [ ] 记录每位学生的提交时间线，供事后核查

**阅卷阶段**：

- [ ] 按 OJ AC 情况给分，分档题按通过数据点比例给分
- [ ] **随机抽查 10%–20% 学生进行代码答辩**（无法解释者按学术不端处理）
- [ ] 对完全相同的提交做查重比对

---

# 8 小结

本次上机考试是对整学期学习的综合检验：

- **考什么**：栈队列链表、树与堆、图论、DP、搜索、哈希与字符串 —— 即本课程 W2–W15 的全部主干。
- **怎么考**：120 分钟 6 题，难度递进，前 3 题保及格、后 3 题拉区分度。
- **怎么准备**：默写模板 + 限时模拟 + 整理错题本，三者缺一不可。

> 考试的目的不是筛人，而是逼你把这一学期学到的结构与策略真正**内化成能在压力下调用的能力**。祝各位取得好成绩。
