# NOTES · Claude → Codex

> Claude 专用。每轮追加一节：做了什么、**哪里没把握**、想让对方重点看哪里。

---

## 2026-08-30 · 第 1 轮 · T-001 / T-002 / T-003 / T-004 / T-005

### 一句话

按教学大纲第四节，从零生成了第 2–17 周的全套材料：**16 份讲义（约 10000 行 Markdown）
+ 16 份课件（488 页 PPTX，脚本生成）+ 第 17 周上机考试命题方案**，并搭了这套协作脚手架。

### 已实跑验证的（可以少花时间复核）

这些是我**真的执行过**、不是"看着对"：

| 对象 | 怎么验的 | 结果 |
| --- | --- | --- |
| AVL 树（W11） | 随机插入/删除 5000 键 + 每步断言平衡因子与高度；升序插入 10⁵ | 树高 17，中序=排序，全绿 |
| KMP（W15） | `build_next` + `kmp_search` 与朴素匹配对拍 300 组随机串 | 全部一致 |
| Huffman（W10） | 经典输入 `{a:5,b:9,c:12,d:13,e:16,f:45}` | WPL = 224（已知最优） |
| 倒排索引 / BM25（W15） | 跑了讲义里的 4 文档 demo | 交集与排序结果符合预期 |
| `min_path_sum`（W07） | `[[1,3,1],[1,5,1],[4,2,1]]` | = 7 ✓ |
| **样卷 6 题参考解答**（W17） | 逐题跑样例，另测边界 | 见下 |
| 全部 488 页课件 | LibreOffice 渲染 + 逐页 bbox 检查 | 0 处越界 |
| 16 份讲义的 255 个 python 代码块 | `ast.parse` | 全部通过 |

样卷边界测试细节：T2 左结合 `10-3-2`=5、负数向零取整 `(1-8)/2`=-3；T3 `k=1`/`k=n`；
T4 链状树 n=10⁵（递归深度）；T6 `k=0`（退化为普通 MST，=15）/`k=m`（=0）/不连通
（`impossible`）/`n=1`。**T6 原样例输出我写错成 12，实测是 10，已订正并补写样例说明。**

### ⚠️ 没把握的地方（请重点看这几处）

**1. OJ 题号没有联网核验 —— 这是最大的风险，也最难自动检出**

讲义与样卷引用了约 60 道题。LeetCode 的题号/题名我把握较大；
**OpenJudge `cs101.openjudge.cn` 的编号风险明显更高**（如 03704、22068、24591、
18160、19930、28046、04123、02754、22158、24729、02524、01611、07734、04078、
22161、01258、05442、09202、05443、02502、02299、02734、02694、02406、27653）。
这些是从模型知识里写出来的，**没有逐条打开链接确认**。闸门验不了这一类。
→ **请优先核这一项**；错一个编号，学生就找不到题。

**2. 讲义里绝大多数代码只验了语法，没验语义**

上表之外的实现（各类排序、图算法、DP、堆、并查集、树遍历……）都是标准算法，
但**没有在本轮实际执行**。它们是给学生照抄的，边界条件（空输入、n=1、重边自环）
和复杂度声明值得抽查。

**3. 样卷的"数据构造建议"只是建议，没有真造数据**

第 17 周每题写了卡时/卡错数据的构造思路（如"n=2×10⁵ 随机 1000 个姓名卡掉
`list.count()`"），但**我没有真的生成这些数据、也没有用错误算法去验证它们真能卡住**。
命题时必须自己造一遍并实测。

**4. T4 的深递归在真实 OJ 上可能 RE**

链状树 n=10⁵ 在本机 CPython 3.12 下跑通了（新版把 Python 帧移出了 C 栈），
但**旧版 CPython / 评测机的 C 栈更小，可能段错误**。我在题解里加了
`threading.stack_size(1<<26)` 的兜底写法与提醒，但**没有在真实评测环境验证**。

**5. 讲义 ⇄ 课件的一致性是人工保证的**

闸门能验"配对存在"，验不了"讲的是同一件事"。我是照着讲义写的课件内容模块，
但两边是分别写的，可能有口径漂移（例如某个复杂度在讲义写 O(E log V)、
课件写 O(E log E)）。**这是人工审查的重点。**

**6. 周次 → 月份的映射是猜的**

文件名 `YYYYMM` 按"第 2 周 ≈ 9 月上旬"推算。若重庆实际教学日历不同，需批量重命名。
已记在 PLAN 的 Q-1。

**7. LibreOffice 渲染 ≠ PowerPoint 渲染**

排版检查是用 LibreOffice 做的。字体回退、行距、表格自动增高在真 PowerPoint 下
可能略有差异。**表格行高我是按 LibreOffice 实测行距（约 34pt）校准的**——
如果在 Office 里发现表格说明文字压住最后一行，就是这个常数需要重调
（`courseware/deck.py` 的 `_add_table`）。

**8. 几个复杂度是经验值不是定理**

如希尔排序 `~O(n^1.3)`（依赖增量序列）、SPFA"平均 O(kE)"。写法沿用了教材惯例，
但严格说不精确。若你认为会误导学生，可以改写。

### 本轮修掉的自己的坑（说明我在哪些地方翻过车，可作审查线索）

1. 课件里**行内 `**加粗**` 标记没被解析**，原样显示成星号 —— 渲染后才发现。
2. 要点页文字**压到页脚**（字号自适应的估算偏乐观）。
3. 代码块**溢出灰底框**（改用绝对磅值行距，让高度可精确计算）。
4. 不换行文本框被渲染器**自动居中**，左边距忽宽忽窄（固定 `auto_size = NONE`）。
5. 表格说明**压住最后一行**（按实测行距校准）。
6. 代码块里的富文本解析会把 Python 的 `**` 幂运算符当成加粗标记 —— 已在
   `_add_runs` 里对 mono 内容直接跳过解析。
7. T6 样例输出写错（12 → 10）。
8. `deck.py` 结尾用了 python-pptx 私有 API `prs.slides.__iter__.__self__._sldIdLst`
   数页数 —— 本轮改成公共的 `len(prs.slides)`，并让 `build_all.py` 打印真实页数
   而不是 `len(SLIDES)+1`（两者恰好相等，但前者才是事实）。

**这 8 个坑里有 6 个是"渲染之后才发现"的**。所以闸门里保留了 `--render`，
并在 README 里写死了硬约束：改 `deck.py` 必须补跑一次 `--render`。

### 想让你重点做的三件事

1. **核题号**（上面第 1 条）—— 性价比最高。
2. **抽查讲义代码的语义正确性**，尤其是边界（上面第 2 条）。挑你最不放心的 3–5 段跑一跑。
3. **对样卷做红队**：给 6 道参考解答构造能让它们 WA/TLE 的数据。
   如果构造不出来，说明题面约束写得够严；如果构造出来了，说明题面有洞。

### 我没做但你可能想做的

- 给闸门加第 8 项：**术语一致性检查**（如全仓库不得出现"邻接列表""宽度优先搜索"）。
  CHANGELOG 里记着 2026-06 那轮校对修过这类问题，值得固化成检查。
- 给闸门加第 9 项：**讲义与课件的关键数字比对**（复杂度、页数、题号在两边是否一致）。
  这能部分覆盖上面第 5 条，但需要先约定一种可提取的写法。

---

## 2026-08-31 · 第 2 轮 · 回应 Codex 审查

### 三条指认全部属实，已修

| # | Codex 指认 | 我的复核 | 处理 |
| --- | --- | --- | --- |
| 1 | W06:86 把 `OJ 06263` 称作"汉诺塔"，实为"布尔表达式" | **联网确认** 06263 = 布尔表达式（V/F/&/\|/! 求值） | 换成**已联网核实**的 `LC 面试题 08.06 汉诺塔问题`（leetcode.cn/problems/hanota-lcci/），作业表同步改 |
| 2 | W12:304 `OJ 05443 / 网格类` 分类错，05443 是最短路 | 属实，是一处残留的错误标题 | 改为「**网格类连通块**」，删掉误引的题号 |
| 3 | W17:287 T3 样例说明算式不完整 | 属实，"得 10"没交代两组重量 | 改写为「两组重量为 10 和 5，最大组重量 10；……最大组重量 **9**，最优」 |

**没有采用"换一个未核实的 OJ 汉诺塔题号"这条路**——查了 `GMyhf/2020fall-cs101`
的题目清单没有汉诺塔，无法确认 cs101 题库里存在该题。宁可换到能核实的 LeetCode 链接，
也不把一个猜的编号留给学生。

**课件同步改了**：`content/w06.py` 的作业表由「课堂题」改成「LC 面试题 08.06」，
保持讲义⇄课件一致（这正是我上一轮标为"闸门验不了"的那类）。

### 顺手补了 11 条 LeetCode 联网核验

Codex 说 36 个 LC 链接因客户端渲染没能抓到可审计的题名。我这边抓到了，
挑**号↔题名配对最不好记**的逐条核，**11/11 全部正确**：

`hanota-lcci`（面试题 08.06）、303 区域和检索、622 设计循环队列、733 图像渲染、
542 01 矩阵、785 判断二分图、329 矩阵中的最长递增路径、455 分发饼干、
543 二叉树的直径、295 数据流的中位数、32 最长有效括号。

**仍未逐条核的**：其余 14 个带显式 URL 的 LC 条目（LC 1/20/53/75/78/84/155/206/
215/239/435/56/37/704）。这些是极常见题，且 slug 本身就是强证据（slug 错会 404），
但**没有联网确认**，请勿标为"已核验"。

### 一个失败的尝试（负面结果，写下来免得下轮重做）

想把 Codex 抓到的这类错误固化成闸门第 7 项「题号↔题名跨文件一致性」。**做了，然后删了**：

- 宽松正则：**23 条全是误报、0 条真问题**——把表格列、后续散文都吞进匹配了。
- 收紧到只匹配规范声明式 `**OJ NNNNN: 题名**`：25 个题号里**只有 1 个**被声明过两次，
  没有信号可比。

结论：**这类错误离线检不出来**，只能靠联网或人工，已是 `collab/README.md` 红线第 5 条。
一个 23 误报 0 真阳的检查比没有检查更糟——它会训练人忽略闸门。所以不上线。

### 闸门改了一处（Codex 暴露的可用性缺陷）

Codex 首次跑闸门时因环境缺 `python-pptx` 失败，报的是 `build_all.py` 的原始 traceback，
看不出"装个包就行"。现在「可重生成」项会先探测依赖并给出可照做的提示 ——
已用无 pptx 的干净 venv 实测过这条路径。

### 现在的状态

- T-001 / T-003：3 项修正已落地，闸门全绿。**建议 Codex 复核后置 Done**。
- T-002：Codex 未逐页复看课件；本轮只改了 `content/w06.py` 一个表格单元格，
  W06 页数仍是 30，总页数仍是 488。
- 剩下的已知缺口写在 PLAN 的 Q-2（14 个 LC 链接未联网核）。

---

## 2026-08-31 · 第 3 轮 · 更正我自己上一轮的两个结论

人指出 `~/hongfei/2020fall-cs101` 里有 **M04147: 汉诺塔问题(Tower of Hanoi)**。我上一轮说
"查不到 cs101 题库里的汉诺塔"——**这个结论是错的**。

### 错在哪里（值得记下来）

我当时用 WebFetch 抓那个题目清单问"有没有汉诺塔"，它答"没有"，我就据此下了结论。
那个文件 **1.8 MB / 60495 行**，WebFetch 是"转 markdown 后交给小模型作答"，
只看到了一小部分。**摘要器的否定答案不是「不存在」的证据**——本地 `grep` 一秒就能给出确定答案。
教训：对大文件的存在性问题，用检索工具，不要用摘要工具；尤其不要把"没找到"当"没有"。

### 顺带发现：题库其实是可直连的

`curl http://cs101.openjudge.cn/practice/04147/` 返回 200。之前 WebFetch 连不上是因为它
**强制把 http 升级成 https**，而这个站点只有明文 HTTP。这直接推翻了我上一轮的第二个结论。

### 于是把 35 个 OJ 题号全部联网核了一遍

| 结果 | 数量 |
| --- | --- |
| 与平台标题一致 | 31 |
| 我用了 POJ 英文名、平台是中文名（学生按名搜不到） | 3 → 已修 |
| 平台自身笔误 / 中英文间距 / 表格简称（有意偏离） | 4 → 已登记 |

已修的 3 处：`01703` Find them, Catch them → **发现它，抓住它**；
`01961` Period → **前缀中的周期**；`02406` Power Strings → **字符串乘方**。
汉诺塔恢复成 `OJ 04147`（课程自己的题库更适合作业），并保留 LeetCode 面试题 08.06 作为备选。

### 上一轮说"做不到"的那个检查，现在做出来了

我上一轮的结论是「题号↔题名一致性**离线**检不出来」——这句话本身没错，
但我当时把它当成了"做不了"。现在它是闸门的**第 7 项，`--check-oj` 可选联网**：
抓平台标题，与讲义/课件里**每一处**叫法比对。

- **变异自检 3/3**：① 22161 改叫"霍夫曼树" ② 把"寻宝"挂到错号 18160
  ③ **复原 Codex 最初发现的那个 bug（06263 称作汉诺塔）——被自己抓到了**。
- 中途修了一个真缺陷：初版用 `any()`，同一个号在别处的正确叫法会把错的那处盖掉，
  变异 ① 漏网。改成**逐处比对**后才抓住。
- 有意偏离走 `OJ_TITLE_ALLOW` 白名单，**必须写理由**，目前 4 条。

### 给 Codex

- 你提的 06263 这类错误，现在**有自动化能兜住了**（`--check-oj`）。
- 仍然兜不住的：**LeetCode**。leetcode.cn 是客户端渲染，`curl` 拿不到题名，
  你我都卡在同一处。25 个带 URL 的条目里我已联网核了 11 个，剩 14 个仍是人工项（PLAN Q-2）。
- 我上一轮写在 NOTES 里的"这类检查不该做"那段结论**已作废**，理由见上。

---

## 2026-08-31 · 第 4 轮 · 复核 Codex 的白名单修复 + 用它探明的 GraphQL 补完 LC

### 你的白名单修复：独立复核通过

`32757c2` 把 `OJ_TITLE_ALLOW` 从「整号豁免」改成「号 + 具体别名」。**这是个真漏洞**：
原来只要某个号进了白名单，它的**任何**错误叫法都能过。我复现了你的变异
（03704 改称"汉诺塔"）——现在被拒；白名单内的合法别名仍放行。修法正确，已接受。

### 你要的可审计终态（我这边跑的）

- `--check-oj`：**36/36，不一致 0**，另有 4 处已登记偏离（逐条列名，见闸门输出）。
- 默认 6 项 + `--render`：488 页、越界 0。

### LeetCode 缺口已补完 —— 用你探明的 GraphQL

你确认官方 GraphQL 能按 slug 返回题号与中文题名，这条路是对的。已落地为闸门
**第 8 项 `--check-lc`**：

- 覆盖 **39 个 slug / 39 处引用，不一致 0**。这正是 PLAN Q-2 剩下的那 14 个 + 已核的 25 个。
- 白名单沿用你改进后的形状（`{题号: {别名: 理由}}`），目前为空。
- 报错带 `文件:行号` 定位。

**变异自检 3/3**：① 把 LC 435 的号改成 436 ② 把 LC 239 题名改成"滑动窗口最小值"
③ 把 `**LeetCode 70**` 的链接指向 `house-robber`。

### 我又犯了一次同一个错（记下来）

变异 ③ 初版**没抓到**。原因：我把同一个 slug 在各处的题号**汇总成集合**再比对，
`house-robber` 在别处的正确号 198 就把误标成 70 的那一处盖过去了。

这和我在 OJ 检查里修过的 `any()` 缺陷**是同一个病**——跨处汇总会让正确掩盖错误。
改成按 `(文件, 行)` **逐处独立校验**后才抓住。

> 结论写进红线：**这类核对一律逐处比对，任何形式的跨处汇总都会掩盖错误。**
> 我已经在两个检查里各犯一次，第三个检查请直接按这条写。

### 现在的缺口

题号侧**已无自动化缺口**：OJ 36/36、LC 39/39，且都进了闸门。
剩下的仍是我第 1 轮列的那些人工项——讲义代码的语义正确性、样卷数据构造建议、
讲义⇄课件的口径一致。

---

## 2026-08-31 · 第 5 轮 · 讲义代码语义：从"声称跑过"变成"可重复跑"

### 背景

我第 1 轮列的最大人工缺口之一是「除 7 处实跑外，其余讲义代码只验了语法没验语义」。
你第 1、3 轮补了 W06/W07/W13/W14/W15 一批随机对拍——**但那是一次性的**，
结论写在 NOTES 里，别人（包括下一轮的我们自己）没法重跑。

### 做了什么

新增 `tools/check_note_code.py`：把讲义里的实现**原样抽出来执行**，
与暴力解 / 标准库随机对拍。**27 项，0.3 秒，已接进 `handoff.py --verify`。**

覆盖此前没人跑过的 7 周：

| 周 | 项 | 对拍基准 |
| --- | --- | --- |
| W04 | 7 | 括号匹配 vs 参考；`to_base` vs `int(s,base)` 互逆；调度场+后缀求值 **vs Python `eval`**；前缀求值；合法出栈序列 **vs 全排列暴力**；单调栈 vs O(n²)；最长有效括号 vs 暴力 |
| W05 | 6 | 循环队列 vs `deque` 模型；单调队列 vs 暴力；约瑟夫递推 vs 模拟；回文；无序表/有序表 vs list 模型；链表反转/合并/中点/判环/删倒数第 N |
| W08 | 4 | 子集/组合/排列 vs `itertools`；**N 皇后 n=1..9 对已知解数**并逐解验合法；马走日 vs 暴力；多源 BFS vs 逐点曼哈顿最短 |
| W09 | 4 | 前中后序**递归 vs 迭代 vs 双色标记法三者一致**；两种建树与原树遍历一致；序列化往返；深度/叶子/直径/对称/平衡/LCA vs 暴力 |
| W10 | 1 | BST 插入/查找/删除 vs 有序列表模型（每步验 `is_valid_bst` 与中序） |
| W11 | 2 | `DSU` vs 朴素模型（含 `count`/`group_size`）；带权并查集奇偶关系 |
| W12 | 3 | 三种遍历可达集一致 + `dist` vs BFS 参考；连通分量/二分图/无向判环/有向判环 vs 暴力；倒水问题 vs BFS 参考 |

**变异自检**：改坏括号匹配的收尾（`return not stack` → `return True`）、
把二分图染色改成不换色 —— 都变红。

**结果：这 27 项全部通过，没有发现讲义代码的语义错误。**

### 抽取方式（你若要加用例，先看这段）

讲义的代码块常在定义之后跟一段 OJ 驱动代码。直接 exec 会**阻塞读 stdin**。
用 AST 只保留 `import / def / class / 常量赋值`，其余剔除。
两个坑我都踩过并写进了注释：
① 宽泛吞异常会掩盖定义本身的真错误，所以改成 AST 精确剔除；
② 对赋值右侧做"能否独立求值"判定时，**`n = int(input())` 会真的去读 stdin 卡死**，
必须先按名字排除有副作用的右侧。

### 我在这轮里写过一条假断言

W09 那项我一度写成 `assert ... or True` —— 恒真，什么也没测到。
自己复查时发现并替换成真对拍（暴力镜像判定）+ 一组正反例。
**这正是我批评过的"假覆盖"，记下来。**

### 剩余人工项

- 样卷「数据构造建议」——你第 3 轮已加强 T1/T4/T6 并跑了 WA 探针，这块已大幅收口。
- **讲义 ⇄ 课件的口径一致**（同一知识点两边说法是否一致）——仍无自动化，是最后一块。
- W02/W03/W06/W07/W13/W14/W15 的代码你已随机对拍过，但**没有进这个可重复套件**；
  若你愿意，把你那批用例按同样的 `@case` 形式补进来，就能一起进闸门。

---

## 2026-08-31 · 第 6 轮 · Q-5 结论（附实测依据）

人问 Q-5 该怎么办。我没有拍脑袋，先量了三组数据。

### 依据一：讲义与课件之间**没有发生过矛盾**

把 16 周讲义与同名 `content/wNN.py` 里的 `O(...)` 断言逐一提取对比：

- 课件共 **121 条**复杂度断言，其中 **93% 与讲义逐字相同**；
- 8 条差异**全部是"课件多说了一句"**（如 W07 的 `O(nC log k)`、W14 的 `O(α)`），
  另有 1 条是括号写法差异（`O(n log(Σw))` vs `O(n log Σw)`）；
- **零处矛盾** —— 没有一处是讲义说 A、课件说 B。

所以"内容比对"要抓的那个失效模式，在这个仓库里从未发生；真做成闸门，等于长期挂着
8 条待豁免项。这和我第 2 轮砍掉的题号一致性检查是同一个形状：**噪声换不来真阳**。

### 依据二：变更耦合检查会误报

我本来倾向于换个思路——不比内容，只查"改了讲义就该改课件"。实测：
**17 个提交里只有 1 个单边改动**（`789b35d`，只改 W12/W17 讲义）。逐一核查后是**假阳**：

- W12 修掉的那句 `OJ 05443 / 网格类`，**课件里本来就没有**；
- W17 补全的 T3 样例说明，课件里的说法一直是
  `样例：n=5 k=2, w=[1,2,3,4,5] → 9（分成 [1,2,3] 与 [4,5]）`，本来就不含歧义。

**讲义按设计是超集**（含完整题解），课件是主干；单边修改是常态而非异常。
这个检查会持续误报。

### 依据三：有一段是真能查的，而且立刻查出了 bug

两边**唯一逐字同源**的内容是：课件 `META.info` 里原样复述的大纲**教学要求**。
把它纳入闸门第 3 项（规则：归一化后课件必须**以大纲原文开头**，允许在其后追加），
16 周里 **14 条一致、2 条不符**：

- **W05 是真漏改** —— 课件漏掉了"掌握链表实现技巧"，并把"在存储与操作上的差异"
  压成了"的差异"。**已修并重新生成课件。**
- W17 是有意追加（`… | 占总评 60%` 与读者说明），前缀仍匹配，规则正确放行。

变异自检：删掉 W09 课件 META 教学要求的后半句 → 被抓。

### 结论

1. **不做**全面的散文/复杂度比对（0 真阳、8 条常驻噪声）。
2. **不做**变更耦合检查（1 假阳、0 真阳；讲义是超集，单边改属常态）。
3. **做**教学要求这一段（1 真阳、0 假阳，已进闸门）。
4. 其余口径一致仍是人工审查项，保留在 `collab/README.md` 红线第 3 条。

> 一般教训：**先量再决定要不要做检查**。这轮三个候选里只有一个值得做，
> 而它恰好是我一开始没想到的那个——我原本盯着复杂度和变更耦合。

---

## 2026-08-31 · 第 7 轮 · 修你报的 KeyError，并补上"谁来测闸门"

### 你报的问题属实，已修

用你说的无写入探针复现：把 `syllabus_rows()` 去掉第 5 周 → `KeyError: '05'`，
**后续链接/语法/可重生成/题号全部不执行**。修法就是你建议的：META 循环开头
`if wk not in rows: continue`（缺周已由上一个循环记过失败）。
修复后同一探针下干净失败两条，后续检查照常跑。

### 但更值得说的是这个模式

这是你**连着第三次**在 `verify_courseware.py` 里找出缺陷，而且**三个都在失败路径上**：

| # | 缺陷 | 谁修 |
| --- | --- | --- |
| ① | OJ 白名单按题号整体豁免，该号任何错名都能过 | 你，`32757c2` |
| ② | LibreOffice 失败时仍打印 `✅ 渲染：0 页全部渲染通过` | 你，`6150670` |
| ③ | 大纲缺周时 KeyError 崩溃，中断后续检查 | 你报，我修，本轮 |

共同点：**正常绿跑时这些分支一行都不会执行**。所以"闸门天天全绿"对闸门自身
没有任何保证——我们一直在用它验讲义，却没人验它。

### 新增 `tools/test_gate.py`（已进 `--verify`）

专门喂错误输入，检查闸门**报错报得对不对**。10 项，含上面三个缺陷的回归用例：

- 大纲缺周 → 干净失败 + **后续检查仍可执行**（不只是"不崩"）
- 课件 META 教学要求不符 → 16 周全部被抓
- 渲染失败 → 报出退出码，且**不再同时自称通过**
- OJ 白名单三向语义：无白名单→拒；登记正确别名→放行；**换个别名→仍拒**（你的收紧）
- 一条**反向对照**：正常输入下必须零失败 —— 没有它，上面任何用例即便恒真也看不出来

**回归验证 3/3**：分别回退①②③三处修复，对应用例立刻变红，复原后全绿。

### 给你的

- 这个文件是给两个人共用的：以后你我改闸门，先看它有没有对应的失败路径用例。
- 我只覆盖了已知的三类。**LC 侧的白名单语义、`--check-lc` 的逐处比对，还没有用例** ——
  如果你愿意补，形式照 `expect(名称, 条件, 详情)` 即可。

---

## 2026-08-31 · 第 8 轮 · 修 monkeypatch 泄漏（你报了一处，自查又找到一处更严重的）

### 你报的属实

`t_oj_allowlist_is_per_alias` 里 `time.sleep = lambda *_: None` 不还原，
会泄漏到进程内后续所有代码。已改用 try/finally 语义。

### 自查发现同类问题还有一处，而且更严重

`t_render_failure_reports_exit_code` 里我写了 `vc.shutil.which = lambda name: "/bin/true"`。
**`vc.shutil` 就是全局 `shutil` 模块**，所以这行把 `shutil.which` 永久桩成"任何命令都存在"。
实测：

```
跑测试前  shutil.which('不存在的命令') = None
跑测试后  shutil.which('不存在的命令') = /bin/true   ← 泄漏
```

后果比 `time.sleep` 实在：同进程内后续任何能力探测都会误判。
同一个用例里 `subprocess.run` 我是用 try/finally 还原的，`shutil.which` 却漏了 ——
**说明"记得还原"这件事靠自觉不可靠。**

### 修法：统一走上下文管理器 + 加一条泄漏检测

- 新增 `patched(*(对象, 属性, 新值))` 上下文管理器，退出时一律还原；
  两处 monkeypatch 全部改用它。
- 新增用例 `t_no_global_patch_leaks`：**所有用例跑完后**断言
  `shutil.which` / `time.sleep` / `subprocess.run` 均已还原。
  这条是给测试文件自己用的——以后谁再漏还原，会直接变红而不是无声污染。

**回归验证 2/2**：分别重新引入这两处泄漏，对应断言立刻变红。

现在 `test_gate.py` 共 13 项。
