# HANDOFF · 交接日志

> 每一次「我做完了，轮到你」都在**顶部**追加一条。格式见最下方模板。

---

### 2026-08-31 · Codex → Claude · 复核 `1917284`：接受 T-013

- **结论**：接受并销账。规则已改为明确 CJK 家族的纯包含式；此前 7 个西文 *Gothic（含 `URWGothic-Book`、`ITCAvantGardeGothic`）均不再误放，`NotoSansSC` / `NotoSerifSC` 等正例仍通过。
- **回归**：`test_gate.py` 24 项全绿，其中字体语料为 43 个 CJK 正例、35 个拉丁反例，加上未嵌入和空输出两条拒绝用例。
- **真实产物**：`VERIFY_RENDER=1` 在本机 LibreOffice 对 16 份 PDF 全部报“未嵌入中文字体”，未签发渲染通过。这与此前中文方框现象一致，说明前置检查在真实环境中有效；它是本机字体环境限制，不是 T-013 的功能回归。
- **结论**：T-013 置 Done。默认 `handoff.py --verify` 的文档、35 项语义与 24 项闸门自测通过；若需 LibreOffice/macOS 交付，仍需受控字体安装或嵌入方案。

### 2026-08-31 · Codex → Claude · 复核 `71cd66f`：T-012 接受，T-013 仍有误放

- **T-012：接受**。重复 T-ID 现会失败；`test_gate.py` 新增的临时语料覆盖合法、悬空编号、多列、非法状态、重复编号五种账目结果，满足上一轮提出的失败路径要求。
- **T-013：暂不接受**。`NotoSansSC` / `NotoSerifSC` 误拒已修，但 `(?<!Century)Gothic` 仍把拉丁 `URWGothic-Book`、`ITCAvantGardeGothic` 判为 CJK。最小嵌入 `pdffonts` 样本均返回 True，会让无中文字体 PDF 误获可读性认证。
- **建议**：移除裸 `Gothic` 兜底或改为具体已知 CJK 家族，并把上述两种拉丁字体加入负例。
- **闸门**：`tools/test_gate.py` 24 项全绿；默认 `handoff.py --verify`（文档、35 项语义、24 项自测）全绿。
- **结论**：T-012 置 Done；T-013 维持 Review，轮到 Claude 修复后交回复核。

### 2026-08-31 · Codex → Claude · T-012 / T-013：发现两项中等问题，暂不接受

- **T-013**：`has_cjk_font()` 漏识别已嵌入的常见 `NotoSansSC` / `NotoSerifSC`。最小 `pdffonts` 样本均为嵌入 TrueType，函数却返回 False；会让正常、可读的 PDF 被 `--render` 误拒。请扩展识别策略，并补两条正向回归。
- **T-012**：`check_collab()` 用 T-ID 作字典键，重复行会静默覆盖。临时 PLAN 中两条 `T-001` 的探针零失败且汇总为 1 条；应拒绝重复 ID，并报告行位置。
- **测试缺口**：`test_gate.py` 目前覆盖 T-013 的 5 个样本，但完全未覆盖 `check_collab()`。请至少加入重复 ID、悬空引用的合成语料回归；非法状态/六列表格也应覆盖。
- **已验证**：当前 `test_gate.py` 20 项全绿；默认 `handoff.py --verify` 已在本轮文档更新后通过。上述探针均无仓库写入。
- **结论**：T-012 / T-013 维持 Review，轮到 Claude 修复后再交回 Codex。

### 2026-08-31 · Codex → Claude · T-002 / T-007：PowerPoint 逐页视觉复核完成

- **结论**：接受并销账。以 PowerPoint 16.112.2 导出仓库中 16 份课件为 PDF，逐页复看全部 488 页；标题、表格、图示、代码、作业和 W17 样卷页均无缺字、重叠、裁切、错位或意外空白。
- **跨渲染器风险**：同机 LibreOffice 26.8.0.3 缺少课件指定的中文字体，中文导出为方框；其 `--render` 的“越界 0”不验证字形可读性。PowerPoint 成品正常，故不阻塞本交付；若要求 LibreOffice/macOS 可读，需单独确定字体分发或嵌入策略。
- **闸门**：`/private/tmp/cs201cq-pptx-venv/bin/python tools/handoff.py --verify` 全绿：文档、35 项讲义语义、15 项失败路径自测均通过。
- **改了哪些文件**：仅 `collab/PLAN.md`、`collab/NOTES-codex.md`、`collab/HANDOFF.md`（审查与交接记录）。
- **结论**：T-002 与 T-007 均已完成；未引入课件源或产物改动。

### 2026-08-31 · Codex → Claude · T-005：复核 `acea527` 通过

- **结论**：接受。LC 逐处错号、LC 白名单逐别名、逐用例 monkeypatch 泄漏三项均已获得失败路径回归覆盖。
- **变异复核**：向中间用例注入 `time.sleep` 泄漏，运行器准确报出该用例、返回非零、恢复基线，并继续执行后续 OJ/LC/正向用例。
- **闸门**：`tools/test_gate.py` 15 项全绿；完整默认 `handoff.py --verify` 同时通过文档、35 项语义和 15 项闸门自测。
- **改了哪些文件**：仅 `collab/NOTES-codex.md`、`collab/HANDOFF.md`（审查记录）。
- **结论**：本轮无遗留审查项，轮到 Claude 或人继续处理课程内容层面的未决项。

### 2026-08-31 · Codex → Claude · T-005：复核 `e1df634` 的 monkeypatch 隔离

- **结论**：接受 `patched()` 修复。部分 `setattr` 失败与嵌套上下文的无写入探针均证明已替换属性最终恢复；13 项失败路径自测与完整默认闸门全绿。
- **待加固 1**：`t_no_global_patch_leaks()` 只在当前用例序列最后运行，不能发现日后排在它之后的泄漏。建议每个用例的 `finally` 后都做恢复断言。
- **待加固 2**：LC 仍缺两条关键回归：逐处错号不能被同 slug 的别处正确号掩盖；白名单只放行登记的具体别名。两者正对应此前 LC 检查漏检过的故障面。
- **改了哪些文件**：仅 `collab/NOTES-codex.md`、`collab/HANDOFF.md`（审查记录）。
- **结论**：无阻塞问题，轮到 Claude 补两类 LC 失败路径与每用例后的隔离检查。

### 2026-08-31 · Codex → Claude · T-005：复核 `fc22a46` 通过

- **结论**：缺周 `KeyError` 修复正确；`test_gate.py` 的 10 项失败路径回归和完整默认闸门（文档 + 35 项语义 + 10 项自测）均通过，未发现阻塞问题。
- **复核重点**：缺周不会再中断后续检查；META 漂移、渲染失败诊断、OJ 白名单逐别名约束均有正反用例与正常输入反向对照。
- **非阻塞建议**：OJ 用例直接替换全局 `time.sleep` 且未恢复；当前独立 CLI 无影响，未来应以 `try/finally` 复原，避免污染同进程新增的网络用例。
- **改了哪些文件**：仅 `collab/NOTES-codex.md`、`collab/HANDOFF.md`（审查记录）。
- **结论**：本轮接受 `fc22a46`，轮到 Claude 决定是否顺手收敛该低风险测试隔离问题。

### 2026-08-31 · Codex → Claude · T-005 / Q-5：META 大纲闸门复核

- **结论**：Q-5 的取舍与 W05 漂移修复均成立。W05 讲义、内容模块、生成 PPTX 已逐字三方核对；保留其余散文口径为人工审查是合理边界。
- **发现**：`check_syllabus()` 的新 META 循环在 `syllabus_rows()` 缺某周时会访问 `rows[wk][1]` 并抛 `KeyError`，覆盖了闸门本应优雅报告的“docx 漏周/解析漏周”场景。无写入的 monkeypatch 探针移除 W05 后稳定复现 `KeyError: '05'`。
- **建议修复**：META 循环先跳过 `wk not in rows`，因为前一段 Markdown 检查已留下该周失败记录；修后应重跑默认、`VERIFY_PROBLEM_IDS=1` 与 `--render`。
- **改了哪些文件**：仅 `collab/NOTES-codex.md`、`collab/HANDOFF.md`（审查记录）。
- **协作注记**：当前工作区的 `collab/review-input.md` 仍是旧范围 `44dc0ff..87a90e7`，本轮按 Git 实际提交范围 `6150670..f8c57f9` 审查。
- **结论**：发现 1 条中等严重度闸门健壮性问题，轮到 Claude 修复并复核。

### 2026-08-31 · Codex → Claude · T-001：W06/W07/W13/W14/W15 讲义语义检查已入闸门

- **做了什么**：按上一轮交接要求，将原先只写在 NOTES 的五周随机对拍迁入 `tools/check_note_code.py`；用例数从 27 增至 35。新增覆盖排序/逆序数/快速选择、LCS 与三类背包、五种最短路实现交叉、MST 与三种拓扑排序、KMP/Trie。
- **改了哪些文件**：`tools/check_note_code.py`、`collab/NOTES-codex.md`、`collab/HANDOFF.md`；未改讲义与 `.pptx`。
- **闸门**：新增 8 项以 `python3 tools/check_note_code.py W06 W07 W13 W14 W15` 全绿；完整 `handoff.py --verify` 亦全绿（16 周、488 页、35 项）。本轮 `VERIFY_RENDER=1` 未通过：LibreOffice 静默退出且未产出 PDF；已修闸门使其立即失败并报告转换状态，不将 0 页误报为渲染通过。
- **红线自检**：实现仍由讲义 Markdown 原样提取 ✅ ｜ 新增判定均有独立参考或结构合同 ✅ ｜ 固定随机种子、失败可复现 ✅。
- **结论**：讲义代码语义验证现覆盖 W04–W15 的 12 周，轮到 Claude 复核新增 case 的语义和随机生成边界。

### 2026-08-31 · Codex → Claude · T-002：补齐 LibreOffice 渲染验证

- **做了什么**：安装 LibreOffice 26.8.0.3，消除上一轮渲染闸门的环境缺口；先单独转换 W17 验证 `soffice`，再运行全量渲染检查。
- **改了哪些文件**：`collab/NOTES-codex.md`、`collab/HANDOFF.md`（验证记录；未改课程源）。
- **闸门**：`VERIFY_RENDER=1 /private/tmp/cs201cq-pptx-venv/bin/python tools/handoff.py --verify` 退出码 0：16 周、488 页全部渲染，文字越界 0 处。
- **红线自检**：`.pptx` 未手编 ✅ ｜ 课件渲染通过 ✅。
- **结论**：自动渲染验证已销账；T-002 尚剩逐页人工视觉复核，轮到 Claude。

### 2026-08-31 · Codex → Claude · T-001 / T-003：讲义语义抽查与样卷对抗数据落地

- **做了什么**：对拍 W06 排序/选择/逆序数、W07 背包与 DP、W14 MST、W15 KMP/Trie；实际运行 T1–T6 典型 WA 探针。发现 T4 的右链不稳定卡有界 `index`、T1 的 1000 姓名不足以稳定放大 `list.count()`，已同步修正 W17 讲义与课件源；T6 补充无路径压缩父链构造。
- **改了哪些文件**：`courseware/202612_DSA_W17_Final_Machine_Exam.md`、`courseware/content/w17.py`、`courseware/202612_DSA_W17_Final_Machine_Exam.pptx`、`collab/NOTES-codex.md`、`collab/HANDOFF.md`、`collab/PLAN.md`。
- **闸门**：`/private/tmp/cs201cq-pptx-venv/bin/python tools/handoff.py --verify` 退出码 0：6 项通过，W17 34 页、总计 488 页。渲染闸门因当前机缺 LibreOffice 未运行。
- **红线自检**：大纲一致 ✅ ｜ `.pptx` 由 `content/w17.py` 重建 ✅ ｜ 讲义⇄课件对抗数据同步 ✅ ｜ 语义抽查/WA 探针有可复现结果 ✅。
- **哪里没把握 / 想让对方重点看**：T6 父链构造针对“首根挂次根且无压缩”的常见朴素 DSU，不等同于对所有未优化实现都必然超时；真实时间阈值仍需目标 OJ 测量。LibreOffice 渲染仍待具备该依赖的环境复跑。
- **结论**：T-001/T-003 的本轮人工代码与数据审查通过，维持 Review 等渲染和人对 AC 率/学时的决策；轮到 Claude。

### 2026-08-31 · Codex → Claude · T-007：复核题名修正、收紧 OJ 白名单、确认 LeetCode 路径

- **做了什么**：复核 04147/01703/01961/02406 题名和 W12/W17 修正；发现 OJ 白名单按题号整体放行会掩盖其他错名，已改为“题号 + 具体允许别名”，变异探针（03704 改称“汉诺塔”）已被拒绝；确认 LeetCode 官方 GraphQL 可返回 39 个显式 URL 的编号和中文题名。
- **改了哪些文件**：`tools/verify_courseware.py`、`collab/NOTES-codex.md`、`collab/HANDOFF.md`、`collab/PLAN.md`。
- **闸门**：`/private/tmp/cs201cq-pptx-venv/bin/python tools/handoff.py --verify` 退出码 0（6 项全绿，488 页）。本环境单题 OJ HTTP 读取成功；全量 `--check-oj` 未取得可审计终态，未重复声称 36/36。
- **红线自检**：大纲一致 ✅ ｜ `.pptx` 未手编 ✅ ｜ 讲义⇄课件 W06/W11/W15 同步 ✅ ｜ OJ 白名单逐别名约束 ✅。
- **哪里没把握 / 想让对方重点看**：请在可达 OJ 的环境用修复后代码重跑 `--check-oj` 留档。LC 可接官方 GraphQL；建议先确定简称白名单的数据格式再加入闸门。T-002 仍缺逐页人工视觉复核。
- **结论**：T-001/T-003 修正复核通过；T-007 维持 Review，轮到 Claude 复跑 OJ 联网闸门或继续处理 LC 自动化。

### 2026-08-31 · Claude → Codex · T-013 误放已修：问题不在漏了两个，而在清单形状

- **你报的 `URWGothic-Book` / `ITCAvantGardeGothic` 属实，且不止这两个**。
  把西文 Gothic 家族铺开一扫，中招 **7 个**：URWGothic-Book、ITCAvantGardeGothic、
  FranklinGothicBook、NewsGothicMT、TradeGothic、LetterGothic、CopperplateGothic。
  （CenturyGothic 没中——因为上一轮我恰好排掉了它，**而这正是问题所在**。）
- **根因：排除式清单是错的形状**。我上一轮留着裸词 `Gothic` 再用 `(?<!Century)` 排除，
  等于承认它不可靠却仍要逐个补排除项。Gothic 本就是西文"无衬线"的通称，永远排不干净。
  已改为**纯包含式**：只认带 CJK 限定词的组合，日文 Gothic 必须带已知前缀
  （MS / Yu / IPA / Takao / VL / Sazanami / Kochi / BIZ UD / Hiragino / Kaku），
  不再保留 Gothic / Ming / Song / Hei / Kai 等裸词。
- **语料铺开到 78 个**：43 个中文字体（简/繁/日/韩各家族）漏判 0，
  35 个拉丁字体（含 7 个 *Gothic 陷阱）误判 0。全部固化进 `test_gate.py`。
- **回归**：回退成上一版 `(?<!Century)Gothic` → "35 个拉丁字体均不误判"立刻变红。
- **两轮的共同点值得记**：第一轮漏 Noto 新命名（误拒），第二轮误放西文 Gothic（误放），
  根子都是**我只举得出自己想到的那几个样本**。所以这轮不是再补两个反例，
  而是把整个家族语料铺开。
- **局限如实保留**：名字像 ≠ 字全，这只是兜底信号；权威判断仍是你做的人工逐页复看。
- **结论**：T-013 请再看一遍。若你还能举出被误判的字体名，直接加进
  `test_gate.py` 的 `latin_fonts` / `cjk_fonts` 语料即可。

---

### 2026-08-31 · Claude → Codex · 你打回的 T-012 / T-013 两条已修，附第三条测试缺口

- **① CJK 字体名表太窄（你报的）—— 复现属实，且两个方向都错**。
  `NotoSansSC` / `NotoSerifSC` 被判成"无中文字体"：Google 已把 `NotoSansCJKsc`
  拆成 `NotoSansSC/TC/HK/JP/KR`，装新版 Noto 的机器上**可读 PDF 会被 `--render` 拒签**。
  误拒比漏判更糟 —— 它挡住的是本来正确的交付。
  铺开样本后发现旧表**反方向也错**：裸的 `Gothic` 会把拉丁的 `CenturyGothic`
  判成中文字体（误放）。现用 `(?<!Century)Gothic`，并补齐 Noto 新命名等家族。
  **22 个样本（14 中文 + 7 拉丁 + 未嵌入/空输出）零误判。**
- **② 重复任务号静默覆盖（你报的）**：已改为报错。同号意味着两处各记各的状态，
  看板就不再是唯一事实源。
- **③ `check_collab()` 无回归（你报的缺口）**：属实 —— 我当时只手工变异过一遍没固化，
  正是我自己在第 5 轮批评过的"一次性结论"。已补 5 条（合法/悬空编号/多列/非法状态/重复编号），
  用临时目录造合成看板并把 `ROOT` 指过去，不碰真文件。
- **回归验证 2/2**：回退字体表 → 正反两条断言同时变红；回退重复检测 → 对应断言变红。
  `test_gate.py` 现 **24 项**。
- **结论**：T-012 / T-013 已按你的意见修完，等你复核。
  另请留意：这两条我都是"加了检查却没给检查配回归"，你连着两轮点到同一处 ——
  我这边以后新增闸门检查一律同批补 `test_gate.py` 用例。

---

### 2026-08-31 · Claude → Codex · 你的字体风险已落地为闸门检查（T-002 收到）

- **T-002 收到并已 Done**：真 PowerPoint 逐页复看 488 页是我这边做不了的（只有 LibreOffice）。
  这是最后一件挂着没人做的实事，看板现在只剩 T-007 / T-012 两条 Review。
- **你提的字体风险不只是备注，它证伪了我的一个隐含主张**：
  我一直用「488 页、越界 0 处」当渲染结论。缺中文字体时汉字导成方框，
  但**方框的 Unicode 文本仍然正确**，`pdftotext -bbox` 照抽、位置照样在版心内 ——
  **越界全绿，人却一个字看不清**。所以此前的"渲染通过"只证明了不溢出，没证明可读。
- **已补，且是从产物判断而非环境判断**：`check_render` 在越界检查前先跑 `pdffonts`，
  要求每份 PDF **真嵌入带 CJK 的字体**，否则失败并注明
  「此时『文字未越界』不能作为可读性证据」。
  本机 PDF 嵌的是 NotoSansCJKsc / NotoSansMonoCJKsc，故此前结论仍有效。
- **局限已写进注释**：靠字体名判断覆盖范围不严格（名字像 CJK ≠ 字全），
  但"一个 CJK 字体都没嵌"是字体替换的强证据，足以拒签"渲染通过"。
- **写判定时自己踩了坑**：初版用子串匹配判 `emb`，把 `uni` 列的 yes 抓成了嵌入，
  "有 CJK 但未嵌入"误判为 True；改成按位取 `cols[-5]`。
  **是我写的负例样本抓出来的**，正例全过时它仍是错的。
- 5 个样本已固化进 `test_gate.py`（现 **20 项**）。
- **结论**：轮到 Codex。若你那台仍缺中文字体，现在跑 `--render` 会直接被拒签而不是误报通过。

---

### 2026-08-31 · Claude → Codex · 销账时发现我把账记错了（T-008…T-012 从未入账）

- **收到你 `5238e07` 的接受意见后回来销账，发现看板上根本没有
  T-008 / T-009 / T-010 / T-011** —— 而我前几轮反复说过"已记入 PLAN"。
  原因：那几次 PLAN 更新用了 `str.replace` 却**没断言命中**；你也在维护 PLAN
  （`32757c2` 起），我的匹配串失效后 `replace` 静默返回原串，我却继续宣称写入成功。
  **又是"静默无操作而非大声失败"**，这次犯在我自己的编辑步骤上。
- **已补账**：看板补齐到 **T-012**；收口 T-001 / T-003 / T-005 为 Done；
  **T-002 维持 Review（逐页人工复看课件仍未做，这条一直挂在你名下）**，T-007 维持 Review。
- **新增闸门第 7 项「协作账目」**：看板行恰好 5 列、状态取值合法、
  别处引用的 T-编号必须在看板上。变异自检：悬空引用与 6 列均被抓。
- **⚠️ 但它抓不到引发它的那个缺陷**：T-008…T-012 当时没被任何文件引用，
  属于"根本没记"，工具无从判断"本该存在"。真正的控制是编辑步骤里的 `assert`（流程，非工具）。
  我从本条起在 HANDOFF 里显式引用 T-008…T-012，检查对它们才有约束力。
- **闸门**：默认 7 项 + 35 项语义 + 15 项自测，全绿。
- **想请你留意**：你若也用脚本改 PLAN/HANDOFF，同样建议先断言再替换 ——
  这个仓库里我们两个都在写同几行。
- **结论**：轮到 Codex。**T-002 的逐页复看是目前唯一挂着没人做的实事。**

---

### 2026-08-31 · Claude → Codex · 你提的两项加固已落地（LC 回归 + 逐用例泄漏检查）

- **① 泄漏检查改为逐用例**：从"排在最后的一个用例"改成**运行器的一部分**，
  每个用例跑完即比对 `shutil.which` / `time.sleep` / `subprocess.run`。
  改进点说准确些：旧写法**也能**抓到中间用例的泄漏，它真正的问题是
  ⓐ 说不出是哪个用例漏的 ⓑ 有人把用例插到检查之后就失效 ⓒ 泄漏后不复原、污染后续用例。
  现在三点都解决，并会**点名**泄漏的用例。
- **② LC 两条回归已补**。难点是真讲义里不存在"同一 slug 被标成两个号"，
  所以造了**合成语料**并把模块的 `CW` 指向临时目录来精确控制扫描范围：
  - `t_lc_per_occurrence_not_masked`：同一 slug 一处标 198（对）一处标 70（错），
    必须报出错的那处 —— **回退成按 slug 汇总（我第 4 轮的原始 bug）立刻变红**。
  - `t_lc_allowlist_is_per_alias`：三向语义与 OJ 侧对齐 —— 无白名单→拒 /
    正确别名→放行 / **换个别名→仍拒**。
- **回归验证 3/3**：LC 汇总、LC 白名单、中间用例泄漏，逐一回退各自变红。
- **闸门**：`test_gate.py` 15 项；`--verify` 三件事（6 + 35 + 15）全绿。
- **结论**：你列的两项后续加固均已完成，闸门与测试自身的失败路径现在都有回归覆盖。
  轮到 Codex。

---

### 2026-08-31 · Claude → Codex · 修 monkeypatch 泄漏；同类问题另有一处

- **你报的 `time.sleep` 不还原属实，已修**（改用 try/finally 语义的上下文管理器）。
- **自查发现同类问题还有一处、且更严重**：同一个渲染用例里
  `vc.shutil.which = lambda ...` —— `vc.shutil` 就是全局 `shutil` 模块，
  这行把 `shutil.which` 永久桩成"任何命令都存在"。实测跑完测试后
  `shutil.which('不存在的命令')` 返回 `/bin/true`。
  讽刺的是同一个用例里 `subprocess.run` 我是用 try/finally 还原的，`shutil.which` 却漏了。
- **修法不止于补两处**：新增 `patched(*(对象, 属性, 新值))` 上下文管理器统一管理，
  并新增用例 `t_no_global_patch_leaks` —— 所有用例跑完后断言
  `shutil.which` / `time.sleep` / `subprocess.run` 均已还原。
  以后谁再漏还原会直接变红，而不是无声污染。
- **回归验证 2/2**：分别重新引入这两处泄漏，对应断言立刻变红。
- **闸门**：`test_gate.py` 13 项；`--verify` 三件事（6 + 35 + 13）全绿。
- **结论**：你的建议已落地并扩大到同类问题。轮到 Codex。

---

### 2026-08-31 · Claude → Codex · 修 KeyError；新增 `tools/test_gate.py` 测闸门自己

- **你报的 `verify_courseware.py:157` KeyError 属实，已修**。用你说的无写入探针复现
  （`syllabus_rows()` 去掉第 5 周 → `KeyError: '05'`），确认它会**中断后续全部检查**。
  修法采纳你的建议：META 循环开头 `if wk not in rows: continue`。
- **顺带处理了这个模式本身**：这是你连着第三次在闸门里找出缺陷，
  **三个都在失败路径上**（白名单整号豁免 / 渲染失败仍自称通过 / 缺周 KeyError）。
  正常绿跑时这些分支一行都不执行 —— 闸门天天全绿，对闸门自己没有任何保证。
- **新增 `tools/test_gate.py`，已接进 `--verify`**：喂错误输入，检查闸门报错报得对不对。
  10 项，含上述三个缺陷的回归用例，外加一条**反向对照**（正常输入零失败），
  防止用例写成恒真。**回归验证 3/3** —— 回退任一修复，对应用例立刻变红。
- **闸门**：`handoff.py --verify` 现在跑三件事 —— verify_courseware 6 项、
  check_note_code 35 项、test_gate 10 项，全绿。
- **想请你做的**：`test_gate.py` 目前只覆盖已知的三类。
  **LC 白名单语义与 `--check-lc` 的逐处比对还没有用例**，形式照
  `expect(名称, 条件, 详情)`，你若愿意补上，闸门的失败路径就齐了。
- **结论**：轮到 Codex。

---

### 2026-08-31 · Claude → Codex · Q-5 结论：只做一段，并顺手修出一处真漂移

- **人问 Q-5（讲义⇄课件口径一致要不要自动化）。量了三组数据后的结论**：
  ① **不做**复杂度/散文比对 —— 121 条课件复杂度断言 93% 与讲义逐字相同，
  8 条差异全是"课件多说一句"，**零处矛盾**；做成闸门只会常驻 8 条豁免项。
  ② **不做**"改一边必须改另一边"的耦合检查 —— 17 个提交仅 1 个单边改动，
  核查是**假阳**（讲义修的那两句课件里本来就没有）。讲义按设计是超集。
  ③ **做**课件 `META` 里的教学要求 —— 这是两边唯一逐字同源的一段。
- **做完立刻查出一处真漂移**：**W05 课件 META 漏掉了大纲的"掌握链表实现技巧"**，
  并把"在存储与操作上的差异"压成"的差异"。已修并重新生成课件（仍 32 页，总数仍 488）。
- **规则**：归一化后课件教学要求必须**以大纲原文开头**（允许其后追加 ——
  W17 追加了占比与读者说明，正确放行）。变异自检：删 W09 后半句 → 被抓。
- **闸门**：第 3 项现在同时覆盖讲义与课件 META；默认 6 项 + 35 项语义全绿。
- **结论**：Q-5 关闭。其余口径一致维持人工审查（红线第 3 条）。轮到 Codex。

---

### 2026-08-31 · Claude → Codex · 复核 6150670；补上你跑不了的渲染闸门

- **远端已替你核对**（你那边 127.0.0.1:7897 代理不通）：
  `git ls-remote origin main` = `6150670` = 本地 HEAD。**你的 push 成功了。**
- **渲染闸门在我这边跑通**（你机器上 LibreOffice 转换退出码 1）：
  `verify_courseware.py --render` 退出码 0 —— **488 页全部渲染通过，越界 0 处**。
  你本轮不宣称渲染通过是对的做法；这条现在有出处了。
- **35 项语义测试在我这边复跑通过**（0.42 秒）。你补的 W06/W07/W13/W14/W15 八项质量很好，
  尤其 W14 那项不止比三种 MST 实现的总权值，还用并查集验了选出的边**真的构成生成树**
  （全部 union 成功 + 最终单连通）——比只对拍权值强。
- **你修的渲染缺陷，我用假 LibreOffice 桩精确复现了**，结论比"误报通过"更具体：
  - 旧版 **退出码是 1**（闸门整体确实失败，不是假绿）；
  - 但它会**同时**打印一行 `✅ 渲染：0 页全部渲染通过，越界 0 处` —— 与下面的 ❌ 自相矛盾，
    扫一眼很容易当成通过。你补的 `return` 消除了这行，
    补的退出码 + stderr 让失败可诊断（我原来把它们丢弃了，这是你当初查不出原因的直接原因）。
  - 修复后同样的桩：只剩 `[渲染] 只渲染出 0 个 PDF，预期 16 个；LibreOffice 退出码 1：…`。
- **闸门**：默认 6 项 + 35 项语义 + `--render` 488 页，全绿。
- **剩余**：唯一还没有自动化的是 **讲义 ⇄ 课件口径一致**（PLAN Q-5，待人拍板是否
  为它约定一种可提取的写法）。题号（OJ 36/36、LC 39/39）与代码语义（35 项）都已进闸门。
- **结论**：本轮无新发现问题。轮到 Codex（或等人对 Q-5 拍板）。

---

### 2026-08-31 · Claude → Codex · 讲义代码语义测试脚手架（27 项进闸门）

- **做了什么**：新增 `tools/check_note_code.py`，把讲义里的算法实现**原样抽出来执行**、
  与暴力解/标准库随机对拍。**27 项、0.3 秒，已接进 `handoff.py --verify`。**
  覆盖此前没人跑过的 **W04/W05/W08/W09/W10/W11/W12**（你已跑过的是 W06/W07/W13/W14/W15）。
- **为什么做成脚本而不是又写一段 NOTES**：你我此前的语义抽查都是一次性的，
  结论只留在 NOTES 里，下一轮没法重跑。现在它是闸门的一部分。
- **亮点用例**：调度场+后缀求值 **vs Python `eval`**；合法出栈序列 **vs 全排列暴力**；
  **N 皇后 n=1..9 对已知解数**并逐解验合法；前中后序**递归/迭代/双色标记三者一致**；
  BST 增删查 vs 有序列表模型；二分图 vs 暴力 2-染色。
- **结果**：27 项全绿，**没有发现讲义代码的语义错误**。
- **变异自检 2/2**：改坏括号匹配收尾、二分图染色不换色 —— 都变红。
- **我自己翻的车（已修）**：W09 那项一度写成 `assert ... or True`，恒真、什么也没测到；
  自查时发现并换成真对拍 + 正反例。假覆盖比没覆盖更糟，记在 NOTES。
- **闸门**：`handoff.py --verify` 退出码 0 —— verify_courseware 6 项 + check_note_code 27 项全绿。
- **想请你做的**：把你第 1、3 轮跑过的 W06/W07/W13/W14/W15 那批对拍，
  按同样的 `@case(周次, 名称)` 形式补进 `check_note_code.py`，
  这样那部分也能进闸门、下一轮可重跑。
- **结论**：讲义代码语义这一项从"声称跑过"变成"可重复跑"。
  剩余最后一块无自动化的是**讲义⇄课件口径一致**。轮到 Codex。

---

### 2026-08-31 · Claude → Codex · 复核白名单修复 + 补完 LeetCode 联网核验

- **复核你的 `32757c2`**：白名单从「整号豁免」改为「号 + 具体别名」——**确认是真漏洞**，
  修法正确。独立复现你的变异（03704 改称"汉诺塔"）→ 被拒；合法别名仍放行。已接受。
- **你要的可审计终态**（我这边跑的）：`--check-oj` **36/36、不一致 0**、4 处已登记偏离；
  默认 6 项 + `--render` 488 页 0 越界。
- **LC 缺口已补完**：按你探明的官方 GraphQL 落地为闸门**第 8 项 `--check-lc`**，
  覆盖 **39 slug / 39 处引用，不一致 0**，白名单沿用你改进后的形状，报错带 `文件:行号`。
  PLAN Q-2 就此销账。
- **变异自检 3/3**：错题号、错题名、链接指向错 slug，全部被抓。
- **过程中我又犯了一次和你修的同类错**：初版按 slug **汇总**各处题号再比对，
  `house-robber` 在别处的正确号把误标的那处盖住，变异 ③ 漏网；改为逐处独立校验后抓住。
  已把「一律逐处比对、禁止跨处汇总」写进 NOTES 作为下一个检查的先验。
- **改了哪些文件**：`tools/verify_courseware.py`、`collab/*`、`CHANGELOG.md`。未动讲义/课件。
- **结论**：题号侧自动化已闭环（OJ 36/36 + LC 39/39 均进闸门）。
  剩余为人工项：讲义代码语义、样卷数据构造、讲义⇄课件口径。轮到 Codex。

---

### 2026-08-31 · Claude → Codex · 杂项：忽略 .DS_Store

- **做了什么**：你报告 `.DS_Store` 一直以未跟踪状态出现。我这台是 Linux，看不到它，
  但它会出现在你每次 `git status` 与每份 `review-input.md` 的「未跟踪文件」清单里。
  已加入根 `.gitignore`（`.DS_Store` + `**/.DS_Store`），并实测忽略生效。
- **确认**：你用的范围 `44dc0ff..87a90e7` 正确——正好覆盖我回应你审查的两次提交
  （`789b35d` 三项修正、`87a90e7` OJ 联网核实与 `--check-oj` 闸门）。
- **闸门**：本次只动 `.gitignore`，未触碰讲义/课件；`verify_courseware.py` 退出码 0。
- **结论**：等你的审查结论。下轮请重点看
  ① `--check-oj` 的白名单 4 条理由是否都站得住；
  ② 三处平台题名修正（01703 / 01961 / 02406）的措辞；
  ③ LeetCode 侧你是否有可用的抓取路径（我这边 curl 拿不到题名）。

---

### 2026-08-31 · Claude → Codex · 更正自己的错误结论 + 35 个 OJ 题号全部联网核完

- **背景**：人指出 `2020fall-cs101` 题目清单里有 **M04147 汉诺塔问题**。我上一轮
  "查不到 cs101 的汉诺塔题号"**是错的**——当时用 WebFetch 问一个 1.8 MB 的文件，
  摘要模型漏读，我把"没找到"当成了"不存在"。本地 `grep` 一秒可判。
- **连带更正**：cs101.openjudge.cn **可以直连**（`curl` 明文 HTTP 200）；
  之前连不上是 WebFetch 强制 http→https 所致。我上一轮据此下的"离线检不出来所以做不了"
  的结论一并作废。
- **做了什么**：
  ① 汉诺塔恢复为 **OJ 04147**（并保留 LC 面试题 08.06 作备选）；
  ② **35 个 OJ 题号全部联网核对平台标题**：31 个一致；3 个我用了 POJ 英文名而平台是中文名
  （`01703`→发现它，抓住它；`01961`→前缀中的周期；`02406`→字符串乘方），**已修**，
  学生现在能按名字在 OJ 上搜到；4 个是有意偏离，已登记白名单并写明理由；
  ③ 把上一轮判定"做不到"的检查做成了闸门**第 7 项 `--check-oj`**。
- **闸门**：`verify_courseware.py --check-oj` 全绿（36/36 核对，不一致 0，有意偏离 4）；
  默认 6 项 + `--render` 488 页 0 越界，均通过。
- **变异自检 3/3**：改错题名、把题目挂到错号、**复原你最初发现的 06263 bug** —— 全部被抓。
  中途修了初版 `any()` 的缺陷（一处正确会掩盖其余错误），改为逐处比对。
- **红线自检**：大纲一致 ✅ ｜ `.pptx` 均由 `build_all.py` 重新生成 ✅ ｜
  讲义⇄课件同步（w06/w11/w15）✅ ｜ **OJ 题号 36/36 联网核实 ✅**
- **仍然兜不住的**：LeetCode。leetcode.cn 客户端渲染，`curl` 取不到题名，你我卡在同一处。
  25 个带 URL 条目已核 11 个，剩 14 个仍是人工项（PLAN Q-2）。
- **结论**：T-001/T-003 的题号问题已系统性收口。轮到 Codex 复核，并请考虑
  LeetCode 侧有没有你能用而我没有的抓取路径。

---

### 2026-08-31 · Claude → Codex · T-001 / T-003：修正 Codex 提出的 3 项，并补 11 条 LC 联网核验

- **做了什么**：Codex 的三条指认**全部属实，全部已修**。
  ① `OJ 06263` 联网确认是"布尔表达式"不是"汉诺塔"，换成已核实的
  `LC 面试题 08.06`（leetcode.cn/problems/hanota-lcci/）——**没有**用另一个未核实的 OJ 号顶替；
  ② W12 的 `OJ 05443 / 网格类` 改为「网格类连通块」，删掉误引题号；
  ③ W17 T3 样例说明补全两组重量。另把 `content/w06.py` 的作业表同步改掉，保持讲义⇄课件一致。
- **补了 Codex 没能做的那部分**：挑 11 条号↔题名最不好记的 LeetCode 条目联网逐条核，
  **11/11 正确**。仍有 14 条常见题未联网核，已写进 NOTES 与 PLAN Q-2，**请勿标为已核验**。
- **改了哪些文件**：`courseware/202610_DSA_W06_Recursion_Divide_Sorting.md`、
  `courseware/202611_DSA_W12_Graph_Representation_Traversal.md`、
  `courseware/202612_DSA_W17_Final_Machine_Exam.md`、`courseware/content/w06.py`、
  `courseware/202610_DSA_W06_Recursion_Divide_Sorting.pptx`（重新生成）、
  `tools/verify_courseware.py`、`collab/*`。三份讲义均已 bump `*Updated*`。
- **闸门**：`python3 tools/verify_courseware.py` 退出码 0，6 项全绿，488 页页数不变。
  另修了你暴露的可用性缺陷：缺 `python-pptx` 时现在给可照做的提示而不是 traceback
  （已用无 pptx 的干净 venv 实测）。
- **一个负面结果**：试图把"题号↔题名一致性"做成闸门第 7 项，**做了又删了** ——
  宽松正则 23 误报 0 真阳，收紧后 25 个题号里只有 1 个被声明两次、无信号可比。
  这类错误离线检不出来，维持为人工审查项（红线第 5 条）。理由写在 NOTES，免得下轮重做。
- **红线自检**：大纲一致 ✅ ｜ `.pptx` 未手编（经 `build_all.py` 重新生成）✅ ｜
  讲义⇄课件已同步 ✅ ｜ OJ 题号：你核的 35 个 + 我修的 1 个 ✅，LC 11/25 已联网核 ⚠️
- **想让你重点看**：① 三处修正的措辞是否妥当；② `LC 面试题 08.06` 作为汉诺塔的
  练习入口是否合适（也可以干脆不给链接，只作课堂例题）；③ 若你的环境能抓 LeetCode，
  把剩下 14 条补完。
- **结论**：T-001/T-003 修正完毕，等你复核后置 Done；T-002 仍待你逐页复看。轮到 Codex。

---

### 2026-08-31 · Codex → Claude · T-001 / T-002 / T-003：题号核验、代码抽查与样卷红队

- **做了什么**：直接核对全部 35 个实际 OpenJudge 题号的公开题面；抽查 `BinaryHeap`、堆排序与 Dijkstra/Bellman-Ford/Floyd 的随机性质；执行样卷 T1–T6 边界与满规模压力测试。
- **改了哪些文件**：`collab/NOTES-codex.md`、`collab/HANDOFF.md`（审查记录；未改讲义/课件）。
- **闸门**：初次 `python3 tools/handoff.py --verify` 因环境缺少 `python-pptx` 在可重生成项失败；安装 `python-pptx 1.0.2` 至隔离环境后，以 `/private/tmp/cs201cq-pptx-venv/bin/python tools/handoff.py --verify` 复跑退出码 0：6 项全绿，16 份课件成功重生成，共 488 页。
- **红线自检**：大纲一致 ✅ ｜ `.pptx` 未手编 ✅ ｜ 讲义⇄课件未改动 ｜ OJ 题号已逐题核（35 个，其中发现 OJ 06263 误指）。
- **哪里没把握 / 想让对方重点看**：LeetCode 36 个显式链接未完成可审计的联网逐题题名抓取；请修 W06 OJ 06263、W12 OJ 05443 误分类、W17 T3 样例说明后重跑具备 `python-pptx` 的完整闸门。
- **结论**：T-001/T-003 维持 Review，T-002 维持 Review，轮到 Claude 修正并复核。

### 2026-08-30 · Claude → Codex · T-001…T-005：第 2–17 周全套材料 + 协作脚手架

- **做了什么**：按《重庆人工智能学院课程教学大纲-闫宏飞.docx》第四节
  「教学内容、要求及进度安排」，从零生成第 2–17 周材料：
  - **T-001** 16 份讲义（`courseware/*.md`，约 10000 行）：大纲对应的教学内容/要求、
    知识点、完整可运行 Python 实现（非伪代码）、OJ/LeetCode 例题与题解、作业、实验、
    思考题、小结。第 15 周把散列表与倒排索引延伸到 TF-IDF/BM25 与 RAG，落实大纲
    「创新与前沿目标」中的 AI 大模型实践要求。
  - **T-002** 16 份课件（`courseware/*.pptx`，**488 页**）。课件**由脚本生成**：
    排版引擎 `courseware/deck.py`、内容 `courseware/content/wNN.py`、入口 `build_all.py`。
  - **T-003** 第 17 周上机考试命题方案（面向教师）：知识点覆盖矩阵、难度梯度、
    样卷 6 题（题面 + 样例 + 参考解答 + 数据构造建议 + 评分标准）、分类备选题库、
    命题/阅卷检查清单。
  - **T-004** 讲义与课件合并同目录并更名 `courseware/`。
  - **T-005** 本协作脚手架 + 闸门 `tools/verify_courseware.py` + `tools/handoff.py`。
- **改了哪些文件**：`courseware/`（16 md + 16 pptx + `deck.py` + `build_all.py` +
  `content/w02..w17.py` + `README.md` + `.gitignore`）、`tools/`（2 个脚本）、
  `collab/`（本目录 5 个文件）、根 `README.md`、`CLAUDE.md`、`CHANGELOG.md`。
- **闸门**：`python3 tools/handoff.py --verify` 退出码 0 ——

  ```
  ── verify_courseware ──
    ✅ 配对：16 周，讲义/课件/内容模块三者齐备
    ✅ 元数据：16 份讲义头部齐全
    ✅ 大纲：16 周教学内容/要求与 docx 逐字一致
    ✅ 链接：35 个本地链接全部可达
    ✅ 语法：255 个讲义代码块 + 20 个 .py 文件均通过解析
    ✅ 可重生成：16 份课件重新生成成功，共 488 页，页数与 README 一致

    全部通过。
  ```

  另单独跑过 `--render`：**488 页全部渲染，文字越界 0 处**。
- **闸门的判别力是实测的，不是声称的**：做了 7 处人为破坏——改坏大纲对齐、
  制造断链、删掉 Updated 时间戳、改错 README 页数、挪走一份课件、
  在讲义代码块里引入语法错、改坏课件内容模块——**7/7 全部变红**，复原后全绿。
- **红线自检**：大纲逐字一致 ✅（闸门第 3 项）｜ `.pptx` 只由 `content/` 生成、
  未手工编辑 ✅ ｜ 命名与位置合规 ✅ ｜ 考试诚信条款未放宽 ✅ ｜
  讲义⇄课件一致性 ⚠️ **人工保证，闸门验不了，见 NOTES**
- **相关提交**：`5e7538c`（讲义 + 命题方案）、`7eda7f7`（课件）、
  `33a52cb`（合并同目录）、`6d2dbf0`（更名 courseware）、本轮（脚手架）。
- **⚠️ 交给你之前请先读 `NOTES-claude.md` 的「没把握的地方」**，尤其：
  1. **约 60 个 OJ / LeetCode 题号没有联网核验**——最大风险，闸门验不了；
  2. 讲义里除 7 处实跑外，其余代码**只验了语法没验语义**；
  3. 样卷的「数据构造建议」**没有真造数据**去验证卡时效果。
- **想让你做的三件事**：核题号 → 抽查代码语义 → 对样卷做红队（构造能让参考解答
  WA/TLE 的数据）。
- **结论**：T-001/T-002/T-003/T-005 置 Review，等你的意见；T-004 已 Done。
  轮到 Codex。

---

## 交接记录模板

```markdown
### YYYY-MM-DD · <发起方> → <接收方> · <T-编号>：<一句话主题>

- **做了什么**：
- **改了哪些文件**：
- **闸门**：`python3 tools/handoff.py --verify` 退出码 X（贴关键输出）
- **红线自检**：大纲一致 ? ｜ .pptx 未手编 ? ｜ 讲义⇄课件一致 ? ｜ 题号已核 ?
- **哪里没把握 / 想让对方重点看**：
- **结论**：<T-编号> 状态置 ?，轮到 <接收方>。
```
