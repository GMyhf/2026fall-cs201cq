# NOTES · Codex → Claude

> Codex 专用。审查意见、发现的问题、不同意见都写在这里。
> 每轮追加一节，标日期与轮次；同时在 `HANDOFF.md` 追加一条交接记录。

## 2026-08-31 · 第 4 轮 · 将 W06/W07/W13/W14/W15 语义对拍纳入闸门

### 结论

已把第 1、3 轮一次性执行过、但此前未进入回归的五周语义检查迁入
`tools/check_note_code.py`。脚本由 27 项扩展为 35 项；新增用例均直接从 Markdown
讲义提取原实现执行，并使用枚举、标准库或独立模型判定。全量 35 项通过。

### 新增可复跑覆盖

- W06：希尔/归并/快排与 `sorted` 对拍；归并逆序数与 O(n²) 枚举对拍；快速选择核第 1、末尾、中位与随机第 k 小。
- W07：两版 LCS 与短串所有子序列枚举对拍；01、多重背包用枚举，完全背包用独立记忆化递归参考。
- W13：堆 Dijkstra 两版、稠密 Dijkstra、Bellman-Ford、Floyd-Warshall 在随机非负图逐源逐项一致。
- W14：Prim 两版、Kruskal 在随机连通图同权，且反查 Prim 选边确为无环连通树；Kahn、字典序 Kahn、DFS 三种拓扑排序逐边验证，并构造双向边环确认都返回 `None`。
- W15：KMP `next` 与真前后缀枚举、匹配位置与朴素搜索对拍；Trie 插入、精确查询和前缀查询合同。

### 验证

- `python3 -m py_compile tools/check_note_code.py` 退出码 0。
- `python3 tools/check_note_code.py W06 W07 W13 W14 W15`：8 项全绿。
- `/private/tmp/cs201cq-pptx-venv/bin/python tools/handoff.py --verify`：16 份课件重生成、488 页与 35 项语义检查均通过。
- `VERIFY_RENDER=1 ... handoff.py --verify` 本轮未通过：LibreOffice 26.8.0.3 静默退出且未产生 PDF。修正 `check_render()` 为 PDF 数量不足时立即失败、附带转换退出码/输出，避免原先在失败旁错误打印“0 页全部渲染通过”。此前 `2ab67b6` 留有一次 488 页渲染成功记录；这次不能据此声称当前渲染已通过。

## 2026-08-31 · 第 1 轮 · T-001 / T-002 / T-003 交叉审查

### 结论

T-001 与 T-003 不能置 Done：发现 3 个应修正文案/题面问题。六份样卷参考解答在合法输入范围内未找到 WA/TLE 反例；T-002 未人工逐页复看，但本轮未改课件源或产物。

### 1. 题号核验

- 已逐页读取 OpenJudge 公开题面，核对了讲义中出现的全部 35 个实际 OJ 号：`01258`、`01611`、`01703`、`01860`、`02299`、`02386`、`02406`、`02502`、`02524`、`02694`、`02734`、`02746`、`02754`、`03704`、`04078`、`04081`、`04123`、`05442`、`05443`、`06263`、`07734`、`09202`、`18160`、`18164`、`19930`、`22068`、`22158`、`22161`、`24591`、`24729`、`27637`、`27653`、`28046`、`28050`、`29803`。除下列一项外，编号与讲义称呼匹配。
- **需修复**：[W06 第 86 行](../courseware/202610_DSA_W06_Recursion_Divide_Sorting.md#L86) 把 `OJ 06263` 称为“汉诺塔”。平台题名实际为“布尔表达式”，该编号必须移除或替换为已核实的汉诺塔题号，不能保留错误链接。
- **需修复**：[W12 第 304 行](../courseware/202611_DSA_W12_Graph_Representation_Traversal.md#L304) 的“`OJ 05443 / 网格类`”不成立：05443 是“兔子与樱花”（最短路），不是网格题。建议删除 `OJ 05443 /`，只保留“网格类”。
- LeetCode 的显式题目 URL 共 36 个；本轮的公开页抓取受客户端渲染限制，未得到可用于逐页题名比对的 HTML 标题。逐处审阅了讲义的号-题名组合，未发现第二个冲突，但这 36 项不应标为“已联网逐题核验”。

### 2. 代码语义抽查

- 从 Markdown 原文提取并随机性质测试：`BinaryHeap` 建堆/插入/弹出与 `sorted` 对拍 100 组；`heap_sort` 与 `sorted` 对拍 100 组；第 13 周 Dijkstra、Bellman-Ford、Floyd-Warshall 在 100 个随机非负有向图上交叉对拍，全部通过。
- 六份样卷解答均运行了针对边界：T1 同次数字典序；T2 左结合、负中间值的向零除法及空格；T3 `k=n`；T4 单结点；T5 完全背包；T6 已建边预合并。全部通过。

### 3. 样卷红队

- 在题面上限实跑参考解答：T1 `n=200000`（1000 个姓名）0.014s；T2 `100 x 1000` 字符表达式 0.030s；T3 `n=100000` 0.121s；T4 `n=100000` 退化链 0.056s；T5 `n=100,C=10000` 0.067s；T6 `n=100000,m=200000` 0.321s。本机 Python 3.14 下均完成。
- 在给定的合法输入约束内，未构造出会使 6 份**参考解答** WA/TLE 的数据。T4 的 `n=100000` 递归链仍有不同评测机 C 栈的 RE 风险，原文已经恰当标注为目标 OJ 实测项。
- **需修复**：[W17 第 287 行](../courseware/202612_DSA_W17_Final_Machine_Exam.md#L287) 样例说明将 `[1,2,3,4]` 与 `[5]` 的第一组重量写为 10；实际是 10 和 5，因此最大值为 10（原句中的“得 10”虽是结果正确，但算式不完整且易混淆）。建议写成“最大组重量为 10”。参考程序与样例输出 9 均正确。

### 闸门

初次 `python3 tools/handoff.py --verify`：配对、元数据、大纲、本地链接、Python 语法均通过；可重生成因当前 Python 3.14 环境未安装 `python-pptx` 而失败。随后在隔离环境安装 `python-pptx 1.0.2`，以 `/private/tmp/cs201cq-pptx-venv/bin/python tools/handoff.py --verify` 复跑，6 项全部通过：16 份课件成功重生成，共 488 页。

---

## 2026-08-31 · 第 2 轮 · T-007 复核 Claude 修正与 OJ 闸门

### 复核结论

- 三处题名/题面修正成立：`OJ 04147` 的公开标题为“汉诺塔问题(Tower of Hanoi)”；`01703` 的“发现它，抓住它”、`01961` 的“前缀中的周期”、`02406` 的“字符串乘方”均应采用平台中文标题；W12 去除将 05443 归为网格类的文字、W17 补全两组重量，措辞正确。
- OJ 白名单的四项理由本身站得住：03704 平台把“括号”误作“扩号”；27653 仅为中英文空格；22158 与 24591 仅为表格简称。

### 发现并已修复的闸门漏洞

原实现按**题号**整体放行白名单。例如把 `OJ 03704` 的讲义题名篡改为“汉诺塔”，也会被“括号/扩号”白名单掩盖。已将白名单收紧为“题号 + 允许的具体讲义别名”。回归探针结果：`03704: 括号匹配问题` 通过；同位置改为“汉诺塔”被拒绝。

### LeetCode 核验路径

LeetCode 的官方 `https://leetcode.cn/graphql/` 可用 `question(titleSlug)` 返回 `questionFrontendId` 和 `translatedTitle`，不依赖客户端渲染。本轮批量读取讲义中 39 个显式题目 URL，均得到平台编号与中文题名；这为补齐 Q-2 剩余项提供了可审计的自动化路径。尚未把该路径直接并入闸门，避免在未约定 LC 题名/简称白名单规则前复制 OJ 的过宽豁免问题。

### 验证与剩余项

- `/private/tmp/cs201cq-pptx-venv/bin/python tools/handoff.py --verify`：6 项全部通过，课件重生成共 488 页。
- 本环境单题 `curl http://cs101.openjudge.cn/practice/04147/` 返回正确标题；但全量 `--check-oj` 在当前执行通道未得到可审计的最终输出，未将其标为本轮全绿。Claude 已记录其环境的 36/36 结果，修复后应在该环境再跑一次留档。
- T-002 的逐页人工视觉复核尚未完成，T-007 维持 Review。

---

## 2026-08-31 · 第 3 轮 · 代码语义与样卷数据红队

### 讲义代码抽查

从 Markdown 原文提取并执行随机性质测试：W06 的希尔/归并/快排/快速选择/逆序数（100 组，和 `sorted` 及暴力逆序计数对拍）；W07 的 01/多重背包（100 组，和枚举对拍）、LCS 两版、最小路径和；W14 的 Prim 稠密/堆优化/Kruskal（100 个随机连通图交叉对拍）；W15 的 KMP（100 组朴素子串匹配对拍）与 Trie 合同。全部通过。本轮没有发现这批可执行讲义代码的语义错误。

### 样卷数据红队

已实际运行六条 WA 探针：T1 同次数字典序（`b,a,b,a`）；T2 左结合 `10-3-2` 与负中间值 `(1-8)/2`；T3 `n=3,k=2,w=1,2,3`（错误的 `< k` 判定输出 6，正确为 3）；T5 `n=2,C=3,v=1,2`（排列数 3，组合数 2）；T6 的已建边免费、断图、输入序贪心三种反例。参考解答均给出正确输出，典型错解均被区分。

### 已加强的数据建议

- T1 改为 2×10^5 个两两不同的合法姓名，稳定放大逐项 `list.count()` 的二次复杂度。
- T4 增加左链 `pre=n..1, inorder=1..n`：其有界 `index` 扫描次数为 n(n+1)/2；原右链只稳定测试递归深度，不足以卡住这一类实现。
- T6 增加零权父链后重复 `(1,n)` 的构造，针对“首根挂次根且未路径压缩”的并查集。

### 闸门

W17 课件已从 `content/w17.py` 重建，仍为 34 页；`/private/tmp/cs201cq-pptx-venv/bin/python tools/handoff.py --verify` 6 项全绿、总计 488 页。`VERIFY_RENDER=1` 未能运行，当前机缺少 LibreOffice；未将其写作渲染通过。

**补充（2026-08-31）**：已安装 LibreOffice 26.8.0.3。以 `/private/tmp/cs201cq-pptx-venv/bin/python tools/handoff.py --verify` 并设置 `VERIFY_RENDER=1` 重跑：488 页全部渲染通过，文字越界 0 处。
